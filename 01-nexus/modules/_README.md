---
tags: [nexus, modules, index]
type: index
created: 2026-05-08
---

# modules/

Atomic notes for the ten firm modules. Every module lives under `apps/worker/src/firm/`. Read each module's `index.ts` first — that's the public surface.

The orchestrator is the stage manager. All opinion comes from Prism / Blade / Shield / Forge / Portfolio Brain — never from the orchestrator itself.

Hub: [[Nexus-MOC]]. Decision path canon: `apps/worker/src/firm/orchestrator.ts:runCycle()`.
