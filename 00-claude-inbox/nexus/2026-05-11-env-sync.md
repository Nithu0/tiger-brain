# 2026-05-11 — .env.example sync

Closed documentation gap surfaced by audit-2026-05-11 finding #1 (gate-silence incident, STRATEGY_BLADE_NEW_GATES silently off Apr 24 → May 11).

## Numbers

- **Code referenced**: 208 distinct env-var identifiers
- **Already documented** (`.env.example` pre-change): 50
- **Missing in code, not in .env.example**: 159
- **Skipped (platform-injected, not operator-configurable)**: 3
- **Added to .env.example**: 156

## Skipped vars + reason

| Var | Reason |
|---|---|
| `GIT_COMMIT_SHA` | Injected by Railway build; not operator-set |
| `RAILWAY_GIT_COMMIT_SHA` | Injected by Railway |
| `RAILWAY_DEPLOYMENT_CREATED_AT` | Injected by Railway |

Tests-only vars: 0 found (all 159 also appeared in non-test code).

## Families added

| Group | Vars | Notes |
|---|---|---|
| Strategy Blade gates | 5 | `STRATEGY_BLADE_*` — the gate-silence incident vector |
| New gates (gates/new-gates.ts) | 6 | `RISK_LEVEL_HARD_GATE_*`, `ENTRY_STACK_COOLDOWN_*` etc |
| Conviction thresholds | 3 | `CONVICTION_MIN_*` |
| Firm Agent Bus | 9 | `AGENT_BUS_*`, `AGENT_*_ENABLED`, lessons/knowledge |
| Individual firm agents | 19 | `FIRM_AGENT_*_ENABLED` + `_COOLDOWN_SEC` + `FIRM_RESEARCH_DRAINER_*` |
| Firm core state | 6 | `FIRM_BLADE_PAUSED`, `FIRM_COLD_START_*`, `FIRM_EPOCH_ISO`, `FIRM_NORMALIZATION_MODE` |
| LLM provider config | 7 | `CLAUDE_*`, `GEMINI_*`, `GOOGLE_API_KEY`, `OPENAI_FALLBACK_MODEL` |
| ORB strategy | 15 | `ORB_*` |
| Scalp Overlap strategy | 13 | `SCALP_OVERLAP_*` |
| Session Breakout strategy | 10 | `SESSION_BREAKOUT_*` |
| Vol Expansion strategy | 9 | `VOL_EXPANSION_*`, `VOL_EXP_*` |
| Legacy scalp/volatile sizing | 9 | `SCALP_*`, `VOLATILE_*`, `DIRECTION_MODE`, `REGIME_SLTP_*`, `SPLIT_TECHNICAL_*` |
| Strategy execution + position mgmt | 8 | `STRATEGY_EXEC*`, `POSITION_MANAGEMENT_*`, `LET_RUN_*`, `EXPOSURE_*`, `LEGACY_XAUUSD_*` |
| Foundation / cold-start / sync | 7 | `OANDA_*`, `STARTING_BALANCE`, `FRED_CACHE_*`, `DEMO_AUTO_DEGRADE_*`, `DRIFT_*` |
| Notifications / reporting | 11 | `DAILY_MORNING_BRIEFING_*`, `STATUS_REPORT_*`, `DISCORD_CLARITY_*`, `CHALLENGE_AGENTS_*`, `FIREHOSE_DIGEST_*`, `MEMORY_RECALL_*`, `MACRO_ANALYSIS_*`, `NEWS_ANALYSIS_*`, `MARKET_PULSE_*`, `LLM_BRIEFINGS_*`, `DISCORD_GREET_*` |
| Bot manager | 2 | `BOT_MANAGER_AUTO_*` |
| Postmortem + raw data + shadow log | 5 | `POSTMORTEM_HOOK_*`, `POSTMORTEM_BACKLOG_*`, `RAW_DATA_PERSIST_*`, `SHADOW_LOG_*` |
| CIO dispatcher | 3 | `NEXUS_CIO_*` |
| FVG detector | 3 | `FVG_*` |
| Misc | 4 | `ANALYTICS_SINCE_ISO`, `QDRANT_COLLECTION`, `STRUCTURE_INCLUDE_TECH_DIR`, `TWELVE_DATA_KEY` |

**Total: 156 vars** across 19 families.

## Format conventions

- All vars commented-out (`#` prefix) so they don't override Railway.
- Default value after `=` is the literal default from code (verified via call site).
- One-line purpose comment per var.
- Grouped by family with `# ─── Family ───` headers.

## Verification

- `tsc --noEmit` (apps/worker): clean (no code touched).
- Re-running `comm -23 /tmp/code-envs.txt <(grep -oE '^[# ]*[A-Z_]+=' .env.example | sed -E 's/^[# ]*//; s/=//' | sort -u)` returns only the 3 Railway-injected vars.
- `git diff --stat .env.example` → 205 insertions.

## Not pushed

Per operator rule. Awaiting `! git push origin main` handoff.
