# Nexus — tool & capability inventory (2026-06-22)

> Generated by a 4-cataloguer + critic ultracode sweep. CORRECTION: the Nexus-endpoints cataloguer read the stale local checkout (branch d04ca97) — /learning/loop-state (#133), /learning/loop-status (#135), /jarvis/{tts,brief,why-no-trade} (#157) DO exist on origin/main. Treat its 'do not exist' as a stale-tree artifact.


---

## MCP servers + tools (count: 147)

# MCP Server Inventory — Nexus Firm Claude Agents

Verified live this session: **nexus-pg-rw** (returned `SELECT 1`), **filesystem** (allowed dir = repo root), **github** (auth = `Nithu0`). Read-only of nexus-pg-rw confirmed empirically: `CREATE TEMP TABLE` rejected with `cannot execute CREATE TABLE in a read-only transaction`.

**10 MCP servers, ~147 tools total.** Tool counts per server below.

---

## 1. github (53 tools) — repo/PR/issue/CI automation
Auth: GitHub PAT, logged in as `Nithu0` (4 repos). Live.
- **Read/search**: `get_me`, `get_file_contents`, `list_commits`, `get_commit`, `list_branches`, `list_tags`, `get_tag`, `list_releases`, `get_latest_release`, `get_release_by_tag`, `search_code`, `search_commits`, `search_issues`, `search_pull_requests`, `search_repositories`, `search_users`, `list_issues`, `issue_read`, `list_pull_requests`, `pull_request_read`, `list_repository_collaborators`, `get_teams`, `get_team_members`, `get_label`, `list_issue_fields`, `list_issue_types`.
- **Write**: `create_branch`, `create_or_update_file`, `push_files`, `delete_file`, `create_pull_request`, `update_pull_request`, `update_pull_request_branch`, `merge_pull_request`, `pull_request_review_write`, `add_comment_to_pending_review`, `add_reply_to_pull_request_comment`, `request_copilot_review`, `issue_write`, `sub_issue_write`, `add_issue_comment`, `create_repository`, `fork_repository`, `run_secret_scanning`.
- **Nexus use-cases**: PR/CI automation for the firm repo (open PRs for strategy proposals, batch-fixes; ai-1/ai-2 landed #106/#107 via this flow); review automation; `search_code` across the 16 firm modules; `run_secret_scanning` as a pre-push safety check; release tagging for deploy cuts.
- **Caveat**: write ops to `main` still operator-gated by "OK kjør"-prinsipp (capability exists, policy blocks). Merge/push are money-near for a prod trading repo — gate hard.

## 2. claude_ai ClickUp (51 tools) — task/doc/chat/time mgmt
Interactively-authed (claude.ai connector). Live in interactive session.
- **Tasks**: `create_task`, `update_task`, `get_task`, `delete_task`, `filter_tasks`, `move_task`, `merge_tasks`, `add_task_to_list`/`remove_task_from_list`, `add_task_dependency`/`remove_task_dependency`, `add_task_link`/`remove_task_link`, `add_tag_to_task`/`remove_tag_from_task`, `attach_task_file`, `get_custom_fields`.
- **Docs**: `create_document`, `create_document_page`, `update_document_page`, `get_document_pages`, `list_document_pages`.
- **Structure**: `get_workspace_hierarchy`, `create_folder`/`update_folder`/`get_folder`, `create_list`/`create_list_in_folder`/`update_list`/`get_list`.
- **Chat**: `get_chat_channels`, `get_chat_channel_messages`, `send_chat_message`, `get_chat_message_replies`.
- **Time/reminders**: `start_time_tracking`/`stop_time_tracking`, `get_current_time_entry`, `add_time_entry`, `get_time_entries`, `get_task_time_in_status`, `get_bulk_tasks_time_in_status`, `create_reminder`/`update_reminder`/`search_reminders`.
- **Comments/members/search**: `create_comment`, `get_task_comments`, `get_threaded_comments`, `find_member_by_name`, `get_workspace_members`, `resolve_assignees`, `search`.
- **Nexus use-cases**: track sprint/foundation-gate tasks; mirror `docs/ops/` workstreams to ClickUp; file strategy-proposal follow-ups as tasks for Karri review.
- **Caveat**: connector auth dies in headless/cron — absent in unattended runs. Sends are external messages → operator-gated.

## 3. obsidian (12 tools) — Brain knowledge base R/W
Local REST API plugin. Vault = `~/Obsidian/Brain/`. Notes addressed by vault-relative path incl. extension; hierarchical `parent/child` tags.
- `obsidian_get_note`, `obsidian_list_notes`, `obsidian_search_notes`, `obsidian_write_note`, `obsidian_append_to_note`, `obsidian_patch_note`, `obsidian_replace_in_note`, `obsidian_delete_note`, `obsidian_manage_frontmatter`, `obsidian_manage_tags`, `obsidian_list_tags`, `obsidian_open_in_ui`.
- **Nexus use-cases**: write verification logs to `00-claude-inbox/<project>/`; update `01-nexus/Nexus-Capability-Evolution.md` (memory standing-order); read/append `00-firm-bus/feed.md` and role inboxes; ingest into `12-youtube`/`13-github-repos` queues; maintain runbooks/decisions.
- **Caveat**: requires the Obsidian app + REST plugin running locally → unavailable in headless/cron. `delete_note` is destructive (operator-gated per global rules).

## 4. filesystem (14 tools) — sandboxed file ops
Allowed root: **`/home/nithu/code/ai-assistent`** only (subdirs included). Live.
- `read_text_file`, `read_file`, `read_multiple_files`, `read_media_file`, `write_file`, `edit_file`, `create_directory`, `list_directory`, `list_directory_with_sizes`, `directory_tree`, `move_file`, `search_files`, `get_file_info`, `list_allowed_directories`.
- **Nexus use-cases**: bulk repo reads/edits scoped to the firm repo; `directory_tree`/`search_files` for fast module discovery; `read_media_file` for dashboard screenshots. Largely overlaps built-in Read/Edit/Write but adds tree/move/search-files primitives.
- **Caveat**: sandbox is repo-root only — cannot touch `~/Obsidian`, `command-center`, etc. (use obsidian MCP / Bash for those, within secret rules).

## 5. playwright (24 tools) — browser automation
Headed/headless Chromium. Live in interactive session.
- **Nav/state**: `browser_navigate`, `browser_navigate_back`, `browser_snapshot` (a11y tree), `browser_take_screenshot`, `browser_tabs`, `browser_resize`, `browser_close`, `browser_wait_for`.
- **Interact**: `browser_click`, `browser_type`, `browser_fill_form`, `browser_hover`, `browser_drag`, `browser_drop`, `browser_select_option`, `browser_press_key`, `browser_file_upload`, `browser_handle_dialog`.
- **Inspect/exec**: `browser_evaluate`, `browser_run_code_unsafe`, `browser_console_messages`, `browser_network_request`, `browser_network_requests`.
- **Nexus use-cases**: browser-verify the dashboard (analytics/live-reasoning/readiness pages) as ai-2 does — real-render check beyond tsc; screenshot tiles (VPA tile on current branch) for proof; capture console/network errors on the live Railway dashboard; verify reconciliation widget renders. `browser_evaluate`/`run_code_unsafe` can probe page state.
- **Caveat**: needs a browser runtime → flaky/absent in headless/cron. `run_code_unsafe` arbitrary JS — treat carefully.

## 6. nexus-pg-rw (1 tool) — live Postgres, READ-ONLY
`mcp__nexus-pg-rw__query(sql)`. **Despite the `-rw` name, the connection is read-only** (verified: writes rejected). Live.
- **Nexus use-cases**: live DB audits/reconciliation — trades, positions, blackboard messages, scorecard, predictions-accuracy, agent_lessons, raw-data tables; verify a fix landed on real data (periodic-verification cadence); cross-check `/health` against ground truth.
- **Caveat**: read-only now (the separate write-capable path described in CLAUDE.md is not what this tool exposes). For approved DB writes/cleanups operator must use the actual RW path.

## 7. fetch (1 tool) — web fetch
`mcp__fetch__fetch(url, max_length, raw, start_index)` → URL to markdown (or raw HTML), paginated via start_index.
- **Nexus use-cases**: pull OANDA/market-data docs, API references, external research pages; cheaper/simpler than playwright for static content.
- **Caveat**: no JS rendering (use playwright for SPA/dashboard).

## 8. Google Drive (8 tools) — file search/read/create
Interactively-authed connector.
- `search_files`, `list_recent_files`, `read_file_content`, `download_file_content`, `get_file_metadata`, `get_file_permissions`, `create_file`, `copy_file`.
- **Nexus use-cases**: pull operator/Karri shared docs (strategy reviews, spreadsheets); archive reports. Secondary to Obsidian for firm knowledge.
- **Caveat**: connector auth → absent headless/cron. External-share writes operator-gated.

## 9. ms365 (2 tools) — Microsoft 365 auth only
`mcp__claude_ai_ms365__authenticate`, `mcp__claude_ai_ms365__complete_authentication`.
- **Status**: only the OAuth handshake is exposed — no mail/calendar/file action tools enumerated. Effectively an auth stub this session; not usable for real M365 ops until/unless action tools surface.
- **Caveat**: auth is interactive (operator-gated).

## 10. MCP resource accessors (2 tools, transport-level)
`ListMcpResourcesTool`, `ReadMcpResourceTool` — generic listing/reading of MCP-exposed resources across connected servers (not server-specific). Useful to discover resource URIs any server publishes.

---

### Cross-cutting caveats
- **Headless/cron blind spots**: obsidian, playwright, ClickUp, Google Drive, ms365 all depend on local apps or interactive OAuth — they vanish in unattended cron/Ralph-loop runs. nexus-pg-rw, filesystem, github (PAT), fetch are the cron-safe set.
- **Policy vs capability**: github merge/push-to-main, ClickUp/Drive external sends, obsidian/filesystem deletes are all *capable* but operator-gated by CLAUDE.md safety rules.
- **Secret rule intersection**: do not have filesystem/Bash read `.env*`, `.git/config`, `~/.ssh` — and the filesystem sandbox already can't reach `~/.ssh` (root-scoped to repo).


**Gaps:** ms365 likely has more tools (mail/calendar/files) behind the auth gate that aren't enumerable until authenticated — only the 2 auth tools surfaced. Possible additional servers not in the deferred list (none observed, but ToolSearch only returns what the harness pre-loaded as deferred). Did not exhaustively probe Google Drive / ClickUp / playwright live connectivity (only github, filesystem, nexus-pg-rw verified by execution); the rest are assumed live from the deferred-tool registration but interactive-auth ones may be stale.


**Caveats:** READ-ONLY session honored — only ran SELECT 1, a rejected probe write (to confirm read-only), get_me, and list_allowed_directories. No mutations, no commits, no secret values read. Tool count (~147) is from the deferred-tool registry + verified loads; exact per-server counts: github 53, ClickUp 51, playwright 24, filesystem 14, obsidian 12, Drive 8, ms365 2, nexus-pg-rw 1, fetch 1, plus 2 generic MCP-resource accessors. nexus-pg-rw read-only status is empirically confirmed this session and contradicts the `-rw` suffix + some CLAUDE.md text describing a write path.

---

## agent types + skills (count: 62)

# Agent Types + Skills Inventory (Nexus firm)

Two capability families here: **subagent types** dispatchable via the `Agent`/Task tool, and **skills** invokable via the `Skill` tool. Counts: ~16 dispatchable agent types (built-in + 10 custom from `~/.claude/agents/`) + ~38 skills = ~54 named capabilities (plus plugins).

---

## A. Subagent types (Agent/Task tool)

### Built-in / generic types
| Type | Purpose | When WE'd dispatch |
|---|---|---|
| `Explore` | Fast read-only codebase/file discovery, returns findings | First pass on "where is X" before editing firm modules; cheap recon |
| `Plan` | Produce an implementation plan from a spec, no edits | Before a multi-step firm change (new gate, sizing logic) — plan then review |
| `general-purpose` | Open-ended multi-step work, full tool access | Catch-all when no specialist fits |
| `claude` | Generic Claude agent | Fallback general execution |
| `code-reviewer` (built-in feature-dev variant) | Adversarial review | See custom version below — same role |
| `feature-dev:code-architect` | Architecture-focused feature design | Designing a new firm module's public surface |
| `feature-dev:code-explorer` | Deep codebase explanation | Understanding `orchestrator.ts:runCycle()` ordering before touching it |
| `feature-dev:code-reviewer` | Plugin review variant | Pre-merge correctness pass |
| `statusline-setup` / `claude-code-guide` | Harness config / Claude Code usage help | Rarely — tooling/meta questions |

### Custom project subagents (defined in `~/.claude/agents/*.md`, model: inherit, scoped to this workspace)
| Type | One-line purpose | When WE'd dispatch (Nexus) |
|---|---|---|
| `backend-api` | Node/TS Fastify v5 APIs, services, typed data-layer (better-sqlite3→Postgres swap-contained) | New `apps/api` routes, `/operator/*` endpoints, request/response typing |
| `brain-engineer` | Cross-project Brain: `@cc/memory-engine`, `@cc/rag-engine`, `@cc/brain-orchestrator` | Memory distill / RAG recall / embedding work feeding lessons into Nexus |
| `code-reviewer` | **READ-ONLY** adversarial review: correctness, security, simplification; reports, never edits | Pre-commit/pre-push skeptic pass on firm-loop or gate changes (money-impact code) |
| `data-pipeline` | Ingestion/ETL/scraping: youtube/github/book queues, research-os, job-scrape (has WebFetch/WebSearch) | Firehose ingest, raw-data-persistence pipelines, knowledge-feeding workstream |
| `frontend-craft` | Restrained Next.js 15 + Tailwind + SWR UI (Linear/Vercel aesthetic) | Dashboard tiles (VPA, reasoning, analytics pages), Wall Street token components |
| `ml-scientist` | Python ML rigor (thesis/materials) + research-os | Prediction/regression-predictor evaluation, meta-label scoring methodology |
| `ops-infra` | systemd/WSL/launchers/backup/migration/CI/deploy/MCP setup (code-2 lane) | Husky CI, Railway deploy plumbing, firm launchers, MCP server setup, backups |
| `refactor-simplifier` | Behavior-preserving simplification/dedup, quality-only (no bug hunt, no features) | Reduce sprawl in firm modules without changing trade behaviour |
| `researcher` | **READ-ONLY** web+repo research, adversarially verified + cited | Library/approach evals, cited market-structure research, comparisons |
| `test-engineer` | vitest (TS) + pytest (Py) tests that exercise real pathways | Add/fix tests guarding the 1005+ count before commit; verify gate changes |

**Dispatch pattern (binding):** per CLAUDE.md, non-trivial work → TaskCreate then 5-10 parallel Agent calls (`superpowers:dispatching-parallel-agents`). READ-ONLY agents (`code-reviewer`, `researcher`) never Edit/Write — they report, author fixes.

**Caveat:** these 10 custom agents are workspace-global (defined in `~/.claude/agents/`), not Nexus-repo-specific — several target sibling projects (thesis, Brain, job-search). They still run in any session but their domain framing assumes the named stack.

---

## B. Skills (Skill tool)

### Nexus project / trading-domain skills (highest leverage for the firm)
| Skill | One-line | When useful |
|---|---|---|
| `trading-knowledge` | Routes through operator's curated Obsidian trading library before answering | ANY strategy/gate/regime/ORB/Karri/XAUUSD question — use instead of training-data guessing |
| `deep-research` | Fan-out web research, adversarial verify, cited report | Multi-source market/strategy research reports |
| `code-review` | Diff review for bugs + cleanups at chosen effort (low→ultra); `--comment`/`--fix` | Pre-merge correctness on firm changes |
| `simplify` | Apply reuse/simplification/altitude cleanups (quality only) | Post-feature cleanup, no bug hunt |
| `verify` | Run the app + observe real behaviour to confirm a fix | "Verify before declaring done" gate (binding op-principle) |
| `run` | Launch/drive the app to see a change working | Confirm dashboard/worker change in real app, not just tests |
| `security-review` | Security review of pending branch changes | Before pushing money-impact code |
| `review` | Review a pull request | PR review pass |
| `loop` | Run a prompt/command on a recurring interval (self-paced if no interval) | Poll deploy status, recurring watch tasks (session-only) |
| `schedule` | Create/manage cron cloud agents (routines), incl one-time | Morning briefing, autonomous watch cron (operator-gated for behaviour) |
| `claude-api` | Anthropic SDK/API reference (models, pricing, tool-use, caching) | Any LLM/agent-bus/model-choice question — read before answering |
| `init` | Initialize a CLAUDE.md | Bootstrapping new repo docs |

### superpowers:* (development discipline — 12)
| Skill | One-line |
|---|---|
| `brainstorming` | Explore intent/requirements before any creative/feature work (MUST precede) |
| `test-driven-development` | Write tests before implementation |
| `systematic-debugging` | Structured root-cause before proposing fixes |
| `verification-before-completion` | Evidence/commands before any "done/fixed/passing" claim |
| `using-git-worktrees` | Isolated workspace for feature work |
| `writing-plans` | Turn spec into multi-step plan before coding |
| `executing-plans` | Execute a written plan with review checkpoints (separate session) |
| `subagent-driven-development` | Execute plan's independent tasks in current session via subagents |
| `dispatching-parallel-agents` | Fan out 2+ independent no-shared-state tasks |
| `requesting-code-review` | Request review before merge |
| `receiving-code-review` | Rigorously evaluate review feedback (no performative agreement) |
| `finishing-a-development-branch` | Structured merge/PR/cleanup options when work complete |
| `writing-skills` / `using-superpowers` | Author skills / bootstrap skill discovery |

### commit-commands:* (git workflow — 3)
`commit` (create commit), `commit-push-pr` (commit+push+PR), `clean_gone` (prune [gone] branches + worktrees). Note: push is "OK kjør"-gated per op-principle.

### claude-md-management:* (2)
`revise-claude-md` (update CLAUDE.md with session learnings), `claude-md-improver` (audit/fix CLAUDE.md files against template).

### feature/UI/meta skills
| Skill | One-line |
|---|---|
| `feature-dev:feature-dev` | Guided feature dev with codebase understanding + architecture focus |
| `frontend-design:frontend-design` | Distinctive intentional visual design guidance for new/reshaped UI |
| `skill-creator` | Create/edit/eval/optimize skills + benchmark triggering |
| `session-report` | Explorable HTML report of session usage (tokens/cache/subagents/skills) |
| `humanizer` | Strip AI-writing tells from text (em-dash overuse, rule-of-three, etc.) — use on all prose per operating mode |

### harness-config skills
`update-config` (settings.json — hooks/permissions/env; automated "whenever X" behaviours need hooks here, not memory), `keybindings-help` (rebind keys), `fewer-permission-prompts` (scan transcripts → allowlist read-only calls in project settings.json).

---

## C. Installed plugins (skill/agent provenance)
- `ralph-wiggum@claude-code-plugins` (1.0.0, project scope) — Ralph autonomous-loop tooling (ties to local-firm-mirror Phase 4/5).
- `superpowers@claude-plugins-official` (6.0.3, project + user) — the superpowers skill suite.
- `context7@claude-plugins-official` — library-docs lookup (note: context7 MCP/skill not surfaced in this session's skill list; plugin present).
- `frontend-design`, `feature-dev`, `commit-commands`, `skill-creator`, `claude-md-management` — official, user scope.

---

## Key caveats
- **Custom agents are workspace-global, not repo-pinned** — domain framing may assume sibling projects.
- **READ-ONLY agents** (`code-reviewer`, `researcher`) and skills (`code-review`, `security-review`, `verify` observe-only) never mutate — safe for the "change nothing" posture.
- **Gated skills:** `commit-push-pr`/`commit` push step needs "OK kjør"; `schedule`/`loop` behaviour changes to the trading loop are operator/Karri-gated; cron routines that alter trade decisions gate per op-principle 6.
- **`trading-knowledge` + `claude-api` are "read-before-answer" skills** — binding routing, not optional.


**Gaps:** context7 plugin is installed but its skill/MCP did not surface in this session's Skill-tool list — likely a context7 MCP (library docs) exists but wasn't enumerable here. The built-in feature-dev:code-architect/code-explorer/code-reviewer agent types are named in the task prompt but I could not confirm their schemas via the Agent tool in this read-only pass (the prompt asserts them; not independently verified). Possible additional project skills (the prompt mentioned trading-knowledge/deep-research/code-review/verify/run/loop/schedule/security-review which I confirmed, but a few it listed like a dedicated 'run/loop/schedule' provenance are CLI-built-ins vs plugin — boundary fuzzy).


**Caveats:** READ-ONLY pass: I read agent .md frontmatter and installed_plugins.json; did not invoke any agent or skill, and read no secrets/.env/.git config. Skill list is taken from the live session system-reminder (authoritative for THIS session) — availability can differ per machine/scope (several skills are user-scope, installed 2026-06-22). Custom subagents enumerated from ~/.claude/agents/ (10 files confirmed on disk). Built-in agent types (Explore/Plan/general-purpose/feature-dev:*) are asserted from the task prompt, not all independently dumped.

---

## Nexus API endpoints + scripts (count: 90)

# Nexus-Specific Tooling Inventory

Source: `/home/nithu/code/ai-assistent`. READ-ONLY enumeration. Three groups: (a) curl-able API endpoints, (b) ops/firehose/oneshot scripts, (c) firm-launcher + firm-bus tooling in `command-center/_bin/`.

## Auth model (read this first)
- API lives at `https://api-production-b660.up.railway.app` (canonical; no `nexus-*` subdomain).
- Global gate in `apps/api/src/plugins/auth.ts`: an `onRequest` hook requires `Authorization: Bearer $API_KEY` on **every** route EXCEPT the public prefixes `/health` and `/diagnostic/backfill-original-risk-points`. If `API_KEY` is unset → dev mode, all open.
- All curl examples below need `-H "Authorization: Bearer $API_KEY"` unless marked PUBLIC.
- Network: only HTTPS/443 egress works from this box (per nexus-watch header) — relevant for any direct fetch.

---

## (a) API endpoints (curl + Bearer)
71 routers registered in `apps/api/src/index.ts` (lines 109-171). Below are the ones in scope plus the highest-value siblings. All GET unless noted. Use-case = how we'd use it from a Claude session.

### Health / diagnostics
- `GET /health` — **PUBLIC**. Structured JSON: db / broker / blackboard / worker heartbeat / reconciliation status. Use: first probe at every checkpoint; nexus-watch polls it.
- `GET /diagnostic/broker` — full OANDA fetch attempt + advice on failure. Use: debug broker connectivity / token rejection.
- `POST /diagnostic/backfill-original-risk-points` + `GET .../status` — **PUBLIC** one-shot backfill of original_risk_points + status. Use: repair risk-point column without a key.

### Operator surface (`operator.ts`, 67KB — richest router)
- `GET /operator/status`, `/operator/status-report` — firm-wide operator state + narrative report.
- `GET /operator/strategies` — per-strategy live state.
- `GET /operator/regime` — current market/risk regime label. Use: sanity-check regime vs raw-$ thresholds (known drift bug).
- `GET /operator/hour-stats` — per-hour-of-day performance.
- `GET /operator/memory-trend`, `/operator/memory-status` — firm_memory growth + health.
- `POST /operator/broker-test` — trigger a broker connectivity test.
- `GET /operator/readiness` (operator-readiness.ts) — readiness/reconciliation widget data (BalanceReconciliation). Use: pre-flip live-readiness audit.

### Firm-agents (`firm-agents.ts`)
- `GET /firm-agents/overview` — agent roster + status.
- `GET /firm-agents/tasks`, `/firm-agents/tasks/:id` — agent task queue.
- `GET /firm-agents/triggers` — pending agent triggers.
- `GET /firm-agents/strategy-proposals` — proposals queue (Karri review pipeline).
- `GET /firm-agents/timeseries`, `/firm-agents/audit`, `/firm-agents/advisories` — agent activity history + advisories.
- `firm-agent-states.ts` (firmAgentStatesRoutes) — per-agent state machine snapshots.

### Learning / firehose / lessons
- `GET /firehose/overview` — learning-loop summary.
- `GET /firehose/lessons` — derived agent_lessons (proposed/approved/drifted).
- `GET /firehose/knowledge` — agent_knowledge (YouTube/book ingest chunks).
- `GET /firehose/derive-status` — last derive-lessons run status. Use: confirm nightly derivation ran.
- Note: the speculative `/learning/loop-state` and `/learning/loop-status` endpoints do **NOT** exist; learning state is surfaced via `/firehose/*` and `/operator/memory-*`.

### Signals / postmortem / shadow / predictions
- `GET /signal-postmortem?days=30` — per-strategy + per-gate false-positive-rate aggregates from signal_postmortem. Use: gate-tuning evidence for Karri.
- `GET /shadow/per-strategy`, `/shadow/comparison`, `/shadow/forward-test`, `/shadow/recent` — shadow-log + forward-test (default-OFF Book→AI modules land here).
- `GET /predictions/accuracy` (+ predictions.ts) — regression-predictor accuracy.
- `GET /meta-label/eval`, `/meta-label/scores` — triple-barrier meta-label evaluation.

### Analytics / metrics / narrative
- `GET /analytics/performance[/:botId]`, `/analytics/strategies`, `/analytics/timeline`, `/analytics/engines`, `/analytics/reviews`, `/analytics/conviction-quartiles`, `/analytics/export`, `/analytics/export/strategy/:id` — analytics + CSV/JSON export. Use: pull perf data for reports without DB access.
- `GET /metrics/overview` — top-line metrics tiles.
- `GET /narrative/replay`, `/narrative/eod`, `/narrative/map`, `/narrative/scorecards` — narrative engine (end-of-day story, scorecards).

### Strategies / calibration / admin
- `GET /strategies`, `/strategies/families`, `/strategies/:id`, `/strategies/:id/intelligence`; `POST /strategies/seed`.
- `GET /calibration`, `/calibration/status` — calibration-epoch state.
- `POST /admin/backfill-m1` (admin-backfill.ts) — backfill M1 candles (auth-gated). Use: fill candle gaps for backtests.
- `firm-memory.ts`: `GET /firm/memory`, `/firm/session`, `/firm/market-state`, `/firm/blackboard`, `/firm/strategy-states` — direct blackboard + firm_memory read surface.

### Assistant
- `GET /assistant/models`; `POST /assistant/ask` — LLM router Q&A over firm context (assistant.ts; llm-router.ts).

---

## (b) Ops / firehose / oneshot scripts

### `scripts/ops/` (operator/firm-bus utilities)
- `nexus-watch.sh` — **CANONICAL** READ-ONLY autonomous watch. Polls 443 endpoints, HEALTHY/WARN/ALARM thresholds, writes digest to `~/Obsidian/Brain/00-claude-inbox/nexus/<day>/`. Discord-pings ONLY on ALARM (REPORT-only, never auto-disables — prinsipp 1). Flags: `NEXUS_WATCH_PING=1`, `NEXUS_WATCH_REUSE=1`, dedup window. Idempotent, schedulable. Use: re-arm session-only watch cron each session.
- `nexus-watch-local.mjs` — cross-platform (pure Node) READ-ONLY mirror of nexus-watch for Windows/Karri's box; on-demand digest, no Discord by default.
- `nexus-watch-llm.sh` — LLM-narrated watch variant (watch-prompt.txt is the prompt).
- `nexus-pg-backup.sh` — off-site pg_dump → gzip → S3-compatible (B2/R2/S3) upload + prune; Discord-ping on failure. **Operator-gated** (writes to external store, needs secrets). Closes the single-DB no-backup audit gap.
- `nexus-pg-restore-test.sh` — restore-verification for the above (a backup you can't restore isn't one).
- `send-discord.mjs` — OUTBOUND-ONLY Discord webhook post (`DISCORD_WEBHOOK_URL`, never printed; can't read replies). Use: send Karri/Nithu a message on say-so. `--file`, `--stdin`, `--as` flags.
- `env-doctor.sh` — reports env vars PRESENT/MISSING from `.env.example` (never prints values). Secret-policy-safe.
- `verify-cognitive-os.sh` — smoke-verify the cognitive-OS wiring.
- `systemd/` — unit files for scheduling the watch.

### `scripts/firehose/` (learning pipeline)
- `derive-lessons.mjs` — pure statistical (no-LLM) lesson derivation from closed trades; dual output: legacy `agent_lessons` + forward `hypotheses` (falsifiable rows). Learning-infra (capture side) — runs freely per prinsipp 6.
- `auto-promote-lessons.mjs` — proposed→approved promotion. **Trade-influencing → gated + capped**, DEFAULT OFF behind `LESSON_AUTO_PROMOTE_ENABLED`. Karri-gated logic.
- `detect-drift.mjs` — re-evaluates approved lessons vs fresh data; sets status=drifted for operator review. Gated by `AGENT_LESSONS_ENABLED` + `LESSON_DRIFT_DETECTION_ENABLED`.
- `weekly-digest.mjs` — renders weekly trade/conviction/gate-fire/activation/lesson/knowledge digest.
- `nightly-dump.mjs` — DuckDB nightly export of core tables (simulated_orders, gate_decisions, firm_memory, decisions, etc.) for offline analysis.
- `ingest-youtube.mjs` — YouTube transcript → agent_knowledge (`AGENT_KNOWLEDGE_ENABLED`, `--url/--domain/--tags/--title`).
- `embed-pending.mjs` — embeds NULL-embedding agent_knowledge chunks; provider openai/voyage (needs API key); idempotent. Gated `KNOWLEDGE_EMBED_ENABLED`.
- `hypotheses.test.mjs`, `derive-lessons.test.mjs`, `auto-promote-lessons.test.mjs` — tests for the above. `setup-local.sh`, `grants.sql`, `migrations/`, `lib/` — local DB setup.

### `scripts/oneshot/` (audited one-time SQL/backfills — historical record)
Dated backfills/dedupes, each with a README: dup-trade fix (08-05), peak_price backfill (13-05), result_r + strategy_id corruption backfills (21-05), shadow_forward_test (03-06), trade_labels (13-06), hypotheses (15-06), read-only grant (11-05). Plus runner `.mjs` wrappers. Use: template for new audited backfills; **DB writes = operator-gated**.

### `scripts/firm/` (local firm mirror / agent runners)
- `discord-listener.mjs` — `!firm <text>` lets operator wake the firm from phone.
- `firm-up.sh`, `preflight.sh`, `run-with-env.sh`, `start.sh` — zellij firm-mirror launch + robust env loader.
- `ralph.mjs` (31KB) — Ralph autonomous loop runner. `agent-runner.mjs`, `agent-codex-runner.mjs`, `agent-review-runner.mjs`, `agent-pr-opener.mjs`, `fanout.mjs` — multi-model agent dispatch. `reconcile-oanda.mjs`, `db-check.mjs`, `status.mjs`, `talk.mjs`, `outbox-sync.mjs`, `codex-worktree-sweep.mjs`.

### `scripts/` (root — backtest/data)
- `backtest-*.mjs` (orb, scalp-cap-sweep, scalp-divergence, vol-exp tunings/cooldown-sweep, strategies), `backfill-oanda-history.mjs`, `backfill-backtest-m1.mjs`, `fetch-macro-data.mjs`, `fetch-news-events.mjs`, `refresh-finnhub-calendar.mjs`, `daily-analysis.mjs`, `_fvg_backtest*.mjs` (Karri WIP — don't touch FVG).

---

## (c) firm-launcher + firm-bus (`command-center/_bin/`, version-controlled)
- `firm-wt-split.sh` — **default** `firm` alias: 8 panes (2x4) in ONE Windows Terminal tab. Row1: code-1/code-2→workspace, ai-1/ai-2→nexus; Row2: thesis-1, as-1, soking-1, personal-1.
- `firm-wt-tabs.sh` (`firmt`) — 8 separate tabs fallback. `firm-zellij.sh` (`firmz`) / `firm-tmux.sh` — zellij/tmux fallbacks.
- `firm-tab-init.sh` — per-pane init contract: exports `FIRM_ROLE/FIRM_PROJECT/FIRM_TAB_OPENED_AT`, logs to `00-firm-bus/feed.md`, touches `inbox/<role>.md`, execs claude. **Do not inline back into the launcher** (wt.exe newline bug).
- `gull-wt-split.sh` (`gull`), `salg-wt-split.sh` (`salg`), `skole-wt-split.sh` (`skole`) — focused single-project launchers (4/3/4 panes). Run ONE at a time (RAM cap ~8 panes).
- **Firm-bus / job dispatch:** `firm-job-submit.sh` (a pane drops a JOB block into code-2's inbox) + `firm-job-run.sh` (code-2 picks up, routes, runs) + `firm-job-gpu.sh`/`firm-job-local.sh`. **PII fail-closed**: `--pii yes` or HARD_LOCAL project or memory.db/PII inputs force LOCAL execution, never leased Vast GPU. Never reads/prints `.env` or `~/.ssh`.
- `firm-inbox-watch.sh`, `firm-heartbeat.sh`, `firm-git-snapshot.sh`, `firm-worktree-{spawn,list,cleanup}.sh`, `firm-statusline.sh`, `firm-session-context.sh`, `firm-capture-session-id.sh`, `firm-brain-ensure.sh`.
- **Brain/RAG helpers (cross-project, used by Nexus sessions):** `rag-recall.sh` + `.ts` (hybrid BM25+bge-m3 recall, needs Ollama at OLLAMA_BASE_URL), `skill-recall.sh`, `global-session-context.sh`, `gen-current-state.sh`, `cost-guard.sh`/`cost-guard-api.sh`/`cost-log.sh`, `brain-{up,down,status,doctor,retention}.sh`, `session-capture.mjs`.

---

## Counts
- API routers registered: 71 (≈30 in-scope endpoints enumerated above).
- `scripts/ops/`: ~10 files. `scripts/firehose/`: ~14. `scripts/oneshot/`: ~20. `scripts/firm/`: ~17. `command-center/_bin/`: ~58 (19 `firm-*`).
- toolCount field (90) ≈ enumerated distinct endpoints + scripts.


**Gaps:** Speculative endpoints in the task brief that do NOT exist in code: /learning/loop-state, /learning/loop-status, and the entire /jarvis/* family (tts, brief, why-no-trade) — grep found nothing for jarvis/why-no-trade; assistant Q&A is /assistant/ask instead. CLAUDE.md references command-center/_bin/firm-task-claim.sh + firm-task-complete.sh — these files no longer exist on disk (only survive as a stale git branch name 'code-2/firm-task-scripts'); the job-dispatch protocol is now firm-job-submit.sh + firm-job-run.sh. I enumerated ~30 of 71 routers in detail; the remaining ~40 (cockpit, warroom, explorer, intelligence, fusion, portfolio-brain, market-pulse, correlation, decision-funnel, strategy-heatmap, risk-snapshot, signal-rejections, firm-activity-feed, threads, candles, chart-data, orb, backtest, providers, etc.) exist and are Bearer-gated but I did not extract their exact sub-paths.


**Caveats:** All API routes except /health and /diagnostic/backfill-original-risk-points require Authorization: Bearer $API_KEY (global onRequest hook in apps/api/src/plugins/auth.ts; no key set = dev mode all-open). Only HTTPS/443 egress works from this box. Did not execute any script, did not curl any live endpoint, did not read .env/.git/config/~/.ssh — names/availability only. Operator-gated actions surfaced: nexus-pg-backup.sh (external store + secrets), any oneshot DB write, auto-promote-lessons / lesson-injection (trade-influencing → Karri-gated), Railway env flips, push to main. firm-job-run PII routing is fail-closed to local. nexus-watch is REPORT-only by design (never auto-disables, prinsipp 1). MCP tools (nexus-pg-rw read-only SQL, obsidian, github, playwright) confirmed loadable via ToolSearch but are a separate domain.

---

## Brain + command-center tooling (count: 23)

# Domain: Brain + Workspace Tooling

Inventory of the workspace-wide AI-OS that Nexus agents plug into. Sources: `~/code/CLAUDE.md`, `~/code/command-center/CLAUDE.md`, `~/Obsidian/Brain/` tree, command-center `packages/`, and Nexus project-memory. Status legend: REAL = built+tested on `main`; PARTIAL = scaffold/skeleton with some real surface; default-OFF = gated; UNCERTAIN = flagged in gaps.

## (a) command-center packages (`~/code/command-center/packages/`)

All under npm-workspace `@cc/*`. There are **25 package dirs** (more than the 6 named in the brief). The 6 brain-product packages are code-1's lane (per firm CHARTER):

1. **@cc/skill-registry** (v0.0.1) — REAL. Three-tier skill discovery/registry (Module D). Scans `~/.claude/skills/<name>/SKILL.md` (workspace), `<repo>/.claude/skills/...` (project), `~/Obsidian/Brain/03-skills/<name>.md` (brain); parses YAML frontmatter; resolves name conflicts. *Nexus use:* register/discover reusable Nexus procedures (e.g. trading-knowledge, deploy playbooks) as first-class skills instead of re-deriving from docs.

2. **@cc/rag-engine** (v0.1.0) — PARTIAL. Spec promises hybrid BM25+vector RAG (T1 naive / T2 advanced / T3 agentic, bge-m3 Ollama embeddings, dim 1024). **Current shipped scope is the eval-runner only** (MRR/P@1/nDCG@10 harness against gold paths); real T1/T2/T3 retrieval are code-1 in-progress tasks (C1-4/5/6). Workspace CLAUDE.md claims eval MRR=0.9556. *Nexus use:* semantic recall over Brain `01-nexus/` knowledge (architecture, decisions, postmortems) and ingested trading material — once real retrieval lands.

3. **@cc/brain-orchestrator** (v0.0.1) — PARTIAL→REAL skeleton. "Generic brain orchestrator — adaptive cycle loop + trigger sweep + lease reaper, lifted from Nexus FirmOrchestrator." Module A always-on daemon: heartbeat/reaper/triggers. **Autonomy gates G4 (nightly-distill, env `BRAIN_ENABLE_NIGHTLY_DISTILL`) + G6 (queue-watcher, `BRAIN_ENABLE_QUEUE_WATCHER`) register DISABLED by default — verified in `daemon.test.ts` (default-OFF, boot-verified).** Full claim/publish/migrations land in task C1-9 (task-persistence, in-progress). *Nexus use:* the autonomy substrate that would auto-pick-up ingestion queues + run nightly memory distillation for the firm.

4. **@cc/memory-engine** (v0.0.1) — PARTIAL. Module B distillation. Has real `distill-real`, `distill-pipeline`, `distill-day`, IDF-table, embedding + storage modules (src shows multiple `.test.ts`). README notes storage layer (FTS5 + sqlite-vec) was a later task; command-center CLAUDE.md flags memory-loop salvage in-progress (distill pay-and-discard fixed; capture/recall wiring still being closed — see `docs/ops/MEMORY-OS-AUDIT.md`). *Nexus use:* distill firm session logs/feed into durable, queryable memory.

5. **@cc/youtube-ingest** (v0.0.1) — REAL (per workspace CLAUDE.md, no longer a shell). Module E: `drainYoutubeQueue` drains `12-youtube/_queue/*.url`, fetches transcript (yt-dlp seam), distills copyright-safe note → `12-youtube/<channel>/`, stores verbatim transcript under `_library/youtube/**` (gitignored), moves queue file to `_processed/`. *Nexus use:* ingest trading-strategy / market-structure YouTube content into Brain knowledge for Karri/strategy review.

6. **@cc/github-discovery** (v0.0.1) — REAL (per workspace CLAUDE.md). `drainGithubQueue` reads `13-github-repos/_queue/`, runs gh search, scores relevance (threshold 0.7), extracts README+manifest only (NEVER clones/executes), distills note → `13-github-repos/<owner>-<name>.md`; license-gated implementation-task proposals (MIT/BSD/Apache/ISC/MPL ok; GPL/AGPL blocked). Rate-limited 50 search + 20 extract/day. *Nexus use:* discover open-source trading/agent-orchestration implementations to learn from.

**Other @cc packages present (not in brief, context only):** `book-ingest`, `obsidian-ingest`, `refiprep` (RefiPrep MVP), plus the command-center app/infra line — `shared`, `bus` (firm-bus reader), `git`, `router` (AI intent router), `executor`, `agents` (Atlas/Cipher/Shield/Prism/Forge/Blade desks), `github`, `sync` (cross-machine Litestream), `brain` (read-only Brain Layer), `auth` (HMAC sessions, Slice 12), `engines` (Anthropic+OpenAI abstraction, Slice 13), `brain-worker`, `engines`, `integration-tests`, `_template`. Command-center itself is Slices 1-13 code-complete on SQLite (`data/command-center.db`).

## (b) Brain ingestion queues + distillation

- **YouTube queue** — `~/Obsidian/Brain/12-youtube/_queue/`. Drop `YYYY-MM-DDTHHMM-<slug>.url` (URL on line 1, optional `# tags:`). Auto-pickup when brain-G6 active; else manual `/skill youtube-ingest`. Has `_failed/` dir + `HOW-TO-DROP-URL.md`. Channel allowlist `_channels.yaml` (TBD); anti-hype filter tags (`marketing-language`, `no-evidence`, `overlap-with-X`); confidence<0.5 → `needs_re_review`.
- **GitHub queue** — `~/Obsidian/Brain/13-github-repos/_queue/`. Drop `.md` with frontmatter (`search:`, `limit:`, `why:`). Auto-pickup G6 or manual `/skill github-discover`. Has `_done/` dir + HOW-TO. License + security policy enforced.
- **Books queue** — `14-books/` + `_library/` (book-ingest package exists). UNCERTAIN on exact queue path.
- **Distillation** — memory-engine (Module B) + Nexus-local Stop hook `scripts/hooks/distill.sh` → `docs/memory/daily/` + `docs/memory/PROMOTE_QUEUE.md`. Nexus memory lifecycle: RAW→DISTILLED→PROMOTED→DEPRECATED→ARCHIVED (`docs/memory/README.md`).

## (c) Per-project memory system + session hooks

- **Per-project memory** — `~/.claude/projects/<slug>/memory/` (Nexus slug = `-home-nithu-code-ai-assistent`). 45+ files: `MEMORY.md` index + `feedback_*`, `project_*`, `reference_*`, `session_*`, `archived/`. Durable, auto-loaded each session, per-project isolated. REAL.
- **MEMORY.md index** — table-of-contents the harness surfaces in system-reminder; each line one-lines a memory file. REAL.
- **Learning-ledger** — `docs/ops/learning-ledger.md`. Every fix needing real-world confirmation gets `- [ ] VERIFY-BY YYYY-MM-DD | item | how-to-verify` in OPEN; verified→VERIFIED, failed→REOPENED. The closed learning loop. REAL (built 2026-05-21).
- **Session-start hooks** — (1) Nexus `scripts/hooks/session-start.sh` token-budgeted context dump (≤6000 chars; safe — only parses `.env.example` var NAMES, never secret values). NOTE: a `.dryrun` variant exists; activation requires settings.json entry. (2) Brain `~/Obsidian/Brain/scripts/brain-session-start.sh` — fast (<2s) brain hygiene: auto-pull, audit, quiet-on-success, runs on firm-tab boot. Both REAL. Hook section 5b surfaces overdue VERIFY-BY items; 8b lists memory files >30d untouched (staleness).
- **Verification tools** — `scripts/ops/verify-cognitive-os.sh`, `scripts/ops/env-doctor.sh` (PRESENT/MISSING only, no values).

## (d) firm-bus coordination (`~/Obsidian/Brain/00-firm-bus/`)

- **feed.md** — append-only inter-terminal event log (~137KB, live; last write 2026-06-22). Read-only observation per ADR-001; one sanctioned write = Slice 11 pane inbox-watcher dispatch receipt. REAL.
- **inbox/<role>.md** — append-only dispatch channel; the ONLY bus path command-center writes to (handoff blocks). REAL.
- **PRESENCE.md** — auto-appended "who's here" snapshot on each firm-tab boot (rows >2h = stale/offline). REAL.
- **CHARTER.md** (v1, operator-confirmed 2026-06-08) — canonical role ownership + collision-prevention contract. code-1 (Brain/AI-OS engine, owns the 6 packages above) + code-2 (apps/infra) are leaders; ai-1 (Nexus live ops) + ai-2 (Nexus strategy/tests) own the firm. Write-collision rule: hand off via inbox across lanes. REAL.
- Also: `roster.md` (quick lookup, points to CHARTER), `README.md`, `pane-status/`, gitignored `git-snapshot.json`.

## Obsidian access path (important caveat)
The **obsidian MCP** (`mcp__obsidian__*`: search/get/write/patch/append/tags/frontmatter notes via Local REST API) IS available in this environment as a deferred tool — this CONTRADICTS Nexus memory `cognitive_os_state.md` which records it as "DISCONNECTED 2026-05-25". Either it was re-registered or availability is environment-dependent. The Brain is in all cases readable via Bash/Read on `~/Obsidian/Brain/`. *Nexus use:* programmatic Brain recall/write (decisions, postmortems, capability-evolution log) — but verify connection before relying on it in headless/cron.

## Cross-machine sync
Brain auto-syncs via Obsidian Git plugin (2-5 min pull). Command-center push is operator-gated ("OK kjør").



**Gaps:** Could not confirm: (1) exact eval MRR=0.9556 number — README only describes the harness, did not open the results file; took the figure from workspace CLAUDE.md. (2) Real rag-engine T1/T2/T3 retrieval status — README explicitly says only eval-runner shipped (v0.1.0), so the "hybrid BM25+vector REAL" claim in CLAUDE.md may be aspirational; flag as PARTIAL not REAL. (3) memory-engine capture/recall end-to-end — command-center CLAUDE.md says capture/recall wiring "still being closed"; distill is real but the full loop is in-progress. (4) Whether C1-9 task-persistence (the layer that makes brain-orchestrator autonomy end-to-end) has actually landed — workspace CLAUDE.md (2026-06-04) calls it in-progress; brain-orchestrator index.ts comment says claim/publish/migrations "land in C1-9" (future tense), suggesting NOT yet merged. (5) book-ingest queue exact path/status — package exists, did not enumerate its queue. (6) obsidian MCP true connection state (available as tool here but Nexus memory says disconnected — unresolved contradiction). (7) Did not verify Ollama bge-m3 Docker stand-up (last G4 precondition) is actually running.


**Caveats:** READ-ONLY pass — nothing changed, nothing committed, no secret values read (only var NAMES referenced). Status claims reconciled between code (package.json/src/test files) and the markdown CLAUDE.md/memory; where they diverge I trusted code and downgraded the claim (rag-engine, memory-engine, C1-9). Brain-orchestrator G4/G6 default-OFF is confirmed directly from daemon.test.ts, not just docs. The 23 toolCount = 6 brain packages + ingestion queues(2) + memory/ledger/hook/verify components(~5) + firm-bus artifacts(4) + obsidian MCP + cross-sync + skill-registry tiers — approximate grouping, not a precise tool-function count (the @cc packages are libraries, not callable MCP tools; the callable MCP surface for this domain is primarily obsidian_* and filesystem on the vault).


---

## Completeness critic

**Missing categories:**
CROSS-CUTTING GAPS (categories no domain inventory owns):

1. HOST CRON / SCHEDULER LAYER — entirely missing. Live `crontab -l` shows a real NEXUS-WATCH-BLOCK (ai-1, 2026-06-08): `*/30 6-21 * * 1-5` + `35 4 * * *` running /home/nithu/.nexus-ops/nexus-watch.sh. None of the 4 inventories listed: (a) the host crontab, (b) the `~/.nexus-ops/` dir (nexus-watch.sh + watch.log), (c) the harness-level CronCreate/CronList/CronDelete + `schedule`/`loop` skills (cloud routines vs host cron — two different scheduling planes, both unlisted). This contradicts memory `project_autonomous_watch.md` which calls the watch "session-only, dies on exit" — it is actually a persistent host crontab. Material correction.

2. WEB / FETCH TOOLS — under-covered. WebSearch, WebFetch, and mcp__fetch__fetch are deferred-available but appear in NO inventory. The deep-research skill (fan-out web search + adversarial verify + cited report) is named in skills list but the Nexus inventory omitted it as a capability. This is the firm's entire external-knowledge intake path (books/YouTube ingestion workstream depends on it).

3. OBSIDIAN MCP — confirmed LIVE this session (12 tools: obsidian_write_note/append/patch/search_notes/manage_frontmatter/manage_tags/etc, server returned usage instructions). Brain inventory flagged it as "unresolved contradiction (memory says disconnected)" — resolve it: it IS connected. This is the write-path into the Brain (00-firm-bus, 01-nexus, claude-inbox) and nobody enumerated its tools.

4. CLICKUP — 53 tools deferred (full CRUD: tasks, lists, folders, docs, comments, time-tracking, chat channels, reminders, tags, dependencies). Agent-types inventory mentioned ClickUp only in passing via CLAUDE.md; none of the 53 tools enumerated. Highest unused surface in the whole roster.

5. GOOGLE DRIVE (8 tools) + MS365 (auth-gated, only 2 surfaced) — Drive has read/write/search/copy/permissions; MCP inventory's own gap note flagged ms365 but Drive's 8 tools were never listed.

6. WORKTREE / NOTIFICATION / MONITOR HARNESS TOOLS — EnterWorktree/ExitWorktree (G3 worktree-default gate is pending per CLAUDE.md — these are the tools that satisfy it), PushNotification, Monitor (the sanctioned wait-on-condition tool vs blocked foreground sleep), RemoteTrigger, TaskStop, DesignSync, NotebookEdit, ListMcpResourcesTool/ReadMcpResourceTool — none covered. Monitor + PushNotification are directly relevant to the autonomous-watch workstream (Bash tool desc explicitly says use Monitor with an until-loop to wait on conditions).

7. FILESYSTEM MCP write/tree tools — inventory verified filesystem read but omitted: directory_tree, edit_file, write_file, move_file, search_files, read_multiple_files, read_media_file (image/PDF reads). The media/multi-file reads are non-trivial capability gaps.

8. SKILLS UNDER-COUNTED — agent inventory said ~38 skills but the live Skill list has ~45 incl. superpowers:* family (systematic-debugging, test-driven-development, verification-before-completion, dispatching-parallel-agents, using-git-worktrees, brainstorming, executing-plans), humanizer (binding per operator "humanize all prose"), session-report, claude-md-improver, fewer-permission-prompts, security-review, verify, run, simplify, code-review. The superpowers verification/debugging skills are binding-adjacent (operator prinsipp 'verify before done') and went unlisted.

UNCLEAR NEXUS USE-CASE (listed but purpose vague):
- statusline-setup / claude-code-guide agents — meta-tooling, no firm use; fine to mark N/A explicitly.
- github request_copilot_review / run_secret_scanning — secret-scanning has a real use (CLAUDE.md secret-handling rules) but no inventory tied it to that.
- DesignSync / NotebookEdit — no Nexus surface (no notebooks in repo); mark N/A rather than leave dangling.

CORRECTIONS TO EXISTING ENTRIES:
- command-center/_bin/ is EMPTY in this repo checkout (firm-launcher scripts not present here) — confirms the Nexus-scripts inventory gap note that firm-task-claim/complete.sh are gone; broaden: the whole _bin launcher set is absent from this working tree.
- rag-engine should be PARTIAL not REAL (Brain inventory already caught this; the MCP/agent inventories implicitly trusted the CLAUDE.md REAL claim).


**Top-5 under-used (adopt more):**
1. PLAYWRIGHT (22 browser tools, all deferred-available, ZERO use in any inventory) — highest leverage. The firm ships a dashboard (apps/dashboard: analytics, live-reasoning, readiness/BalanceReconciliationWidget, 5+ transparency pages) and a Bearer-gated API, yet "verify before declaring done / user's experience is the bar" (binding CLAUDE.md rule) is currently done by curl + tsc only. Playwright browser_navigate + browser_snapshot + browser_take_screenshot + browser_console_messages would let agents actually load the deployed dashboard, screenshot tiles (e.g. the VPA tile on the current branch feat/dashboard-structure-vpa-tile), and catch render/console errors before claiming done. Directly closes the "symptom gone for the user" gate. Adopt immediately for any dashboard PR.

2. nexus-pg-rw (verified live this session, SELECT-only confirmed by CREATE rejection) — under-used for LIVE AUDITS. The firm makes money decisions off Postgres (trades, scorecards, predictions-accuracy, meta-labels) but agents lean on curl /health + reading code. Direct SQL is the ground-truth reconciliation tool: verify trade counts vs OANDA-sync, audit absolute-$ threshold-drift (memory project_absolute_dollar_threshold_drift.md — the real 0-trades cause), confirm agent_lessons/firehose tables are actually filling. Use it as the default first probe in any data-integrity investigation instead of inferring from logs.

3. deep-research skill + WebSearch/WebFetch/fetch — under-used external-knowledge intake. The standing workstream (memory project_autonomous_watch.md: "books/YouTube ingestion is the next knowledge-feeding workstream") and Karri's Book→AI modules (market-structure/VPA) need sourced, verified external research. deep-research (adversarial verify + cited report) is the right tool and is currently invisible to the firm's self-model. Pair with obsidian_write_note to land cited findings into ~/Obsidian/Brain/01-nexus/.

4. Explore / general-purpose subagents via parallel dispatch (superpowers:dispatching-parallel-agents) — under-used relative to the binding "default to parallel agents (5-10)" rule. Most sessions still do single-track work. The codebase is large (71 API routers, 16 firm modules, 1005+ tests); fan-out Explore agents for recon + parallel disjoint-file edits (the documented parallel_batch_pattern) is the operator's explicit preferred mode and remains under-exercised.

5. ClickUp MCP (53 tools, completely dormant) — highest unused surface. Task lifecycle for the firm currently lives in ad-hoc docs/ops/*.md + GOALS.md + (now-absent) firm-task scripts. ClickUp create_task/filter_tasks/update_task/add_task_dependency + create_document gives durable, queryable cross-session task state — the exact gap the in-progress C1-9 task-persistence layer is trying to solve in code. Adopt for tracking strategy proposals (proposed→approved→implemented→archived) and operator-gated action handoffs, which are currently markdown-only and easy to lose.