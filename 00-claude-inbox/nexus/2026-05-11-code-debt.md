---
date: 2026-05-11
type: audit
project: nexus
agent: code-debt-deep
---

# Code debt + TODO audit — 2026-05-11

**Codebase:** 73,602 lines across 3 apps + 1 shared package.
**Grade:** B- (mature, but config documentation dangerously out of sync with code).

## Big finding: 59 undocumented env-flags

**Code uses 62 active feature flags. `.env.example` documents only 3.** Operator visibility into safety-critical gates is near-zero.

Examples of flags live in code but invisible in `.env.example`:
- `STRATEGY_BLADE_ENABLED` (master gate — apps/worker/src/firm/strategy-blade.ts:68)
- `STRATEGY_BLADE_NEW_GATES` (line 132)
- `STRATEGY_BLADE_RISK_VETO` (line 115)
- `STRATEGY_BLADE_EVENT_POLICY` (line 88)
- `STRATEGY_BLADE_FORGE_CHECK` (line 194)
- `FIRM_AGENT_FILL_QUALITY_ENABLED`
- `RANGING_CONVICTION_GATE_ENABLED`
- `NEXUS_CIO_ENABLED`
- ...55 more

**Risk:** A flag defaulting to `false` may silently disable safety gates. Last week's gate-silence incident is a direct symptom — `STRATEGY_BLADE_NEW_GATES` was undocumented + off, gates stopped writing for 14 days unnoticed.

## TODO/FIXME markers

Only 2 explicit markers:
1. `apps/worker/src/firm/managers.ts:469` — `virtualBalance = 10_000; // TODO: source from bots.balance when multi-account lands` — blocks multi-account scaling
2. `apps/api/src/routes/diagnostic.ts:100` — doc comment, not blocking

## Always-false flag candidates (dead code)

1. **`LEGACY_XAUUSD_EXECUTION_ENABLED`** — hardcoded path expects false, 137 refs across codebase
   - `apps/worker/src/agents/trading-manager.agent.ts:50`
   - `apps/worker/src/firm/foundation-gate.ts:224`
   - `apps/api/src/routes/operator.ts:56`
2. **`DISCORD_LEGACY_ENABLED`** — default false, services/discord.service.ts:10 has dead-feature comment

## Console.log in production code

- `apps/worker/src/`: 48 console.log statements (17 in `index.ts` alone — boot/shutdown/subprocess pings)
- `apps/api/src/`: 7 console.log statements
- Should route via `logger.service.ts` for structured CloudWatch output

## Top 5 close-out targets

1. **Sync 59 missing flags to .env.example** (2h, HIGH impact) — extract all `envBool/envInt/envFloat` calls + add docstrings. Stops future silent misconfig like the gate-silence incident.
2. **Replace console.log in worker/index.ts with logger.service** (1h) — reduces CloudWatch noise.
3. **Decide on LEGACY_XAUUSD_EXECUTION path** (1.5h) — commit to remove or document why it persists.
4. **Multi-account balance TODO → ticket** (30min) — formalize from inline comment.
5. **Test coverage for strategy-blade gates** (2h) — 0 unit tests on a critical safety path.

## Bottom line

The codebase is mature, but `.env.example` drift is a real operational hazard. The gate-silence incident proves it. **#1 (flag sync) is the highest-leverage one-day task** — single PR, no behavior change, restores operator visibility.

---
Linked to: [[Nexus-MOC]], [[Foundation-Gate]], [[gate-silence-2026-05-08]]
