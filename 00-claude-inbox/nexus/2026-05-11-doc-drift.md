# Doc drift audit — 2026-05-11

Auditing `/home/nithu/code/ai-assistent/docs/ref/*` against actual code in
`apps/worker/src/` and `apps/api/src/`. Principle: **code wins**. Recommendation
column says whether to update doc or update code.

Verification queries used: `grep` for `process.env.X`, `envBool|envFloat|envNum|envInt|envString`
across `apps/worker/src` + `apps/api/src` excluding `*.test.ts`.

---

## 1. `docs/ref/feature-flags.md` vs code

### Drift findings

| # | Drift | Authority | Recommended action |
|---|---|---|---|
| 1.1 | Entire strategy-module flag families MISSING from doc: `STRATEGY_BLADE_*` (5 flags), `STRATEGY_EXECUTION_ENABLED`, `STRATEGY_EXEC_PAPER_ONLY`, `SCALP_OVERLAP_*` (~15), `SESSION_BREAKOUT_*` (~10), `VOL_EXPANSION_*` / `VOL_EXP_*` (~10), `FVG_*` (3). All active in code via `envBool`/`envFloat` in `firm/strategy-blade.ts`, `firm/scalp-overlap/config.ts`, `firm/session-breakout/config.ts`, `firm/vol-expansion/config.ts`, `firm/fvg-detector.ts`. | code | **Update doc.** Add a "Strategy modules (TIER 3)" section with the per-strategy flag table. |
| 1.2 | Entire firm-agent flag family MISSING from doc: `FIRM_AGENTS_ENABLED`, `FIRM_AGENTS_TICK_BUDGET_SEC`, `FIRM_AGENTS_FALLBACK_ENABLED`, all `FIRM_AGENT_*_ENABLED` + `*_COOLDOWN_SEC` flags (15+). These ARE documented in `env-vars.md` though. | code | **Update feature-flags.md** to xref env-vars.md or duplicate the firm-agent table. |
| 1.3 | Missing from doc: `AGENT_BUS_ENABLED`, `AGENT_TRIGGER_PUBLISH_ENABLED`, `AGENT_DISCORD_DELIVERY_ENABLED`, `DISCORD_CLARITY_ENABLED`, `FIRM_RESEARCH_DRAINER_ENABLED`/`_COOLDOWN_SEC`, `FIRM_BLADE_PAUSED`, `AGENT_KNOWLEDGE_ENABLED`, `AGENT_LESSONS_ENABLED`, `LESSON_DERIVATION_ENABLED`, `LESSON_INJECTION_ENABLED`, `KNOWLEDGE_INJECTION_ENABLED`, `FIREHOSE_DIGEST_ENABLED`, `LET_RUN_ENABLED`, `REGIME_SLTP_ENABLED`, `SHADOW_LOG_ENABLED`, `POSITION_MANAGEMENT_SHADOW_LOG`, `BOT_MANAGER_AUTO_PAUSE_ENABLED`, `BOT_MANAGER_AUTO_RESTART_ENABLED`, `RANGING_DIRECTION_MODE`. | code | **Update doc.** All shipped post-2026-04-25 (ORB pivot) and never back-filled. |
| 1.4 | `PRISM_SYNTHESIS_ENABLED` listed as "Reserved" in doc — confirmed: zero references in code. | (n/a) | Doc is accurate (still reserved). Optionally drop the row. |
| 1.5 | `MEMORY_RECALL_ENABLED` listed; check: `process.env.MEMORY_RECALL_ENABLED` appears in code. | match | None |
| 1.6 | All ORB-pivot kill-switches (`CHALLENGE_AGENTS_ENABLED`, `LLM_BRIEFINGS_ENABLED`, `MACRO_ANALYSIS_ENABLED`, `NEWS_ANALYSIS_ENABLED`, `MARKET_PULSE_ENABLED`, `MEMORY_RECALL_ENABLED`, `ORB_ONLY_MODE`) — all present in code. | match | None |

### Quick scoring

Doc is "correct + incomplete" — what IS listed largely matches reality, but ~50+ flags shipped after 04.25 are completely undocumented. **Highest-impact gap: strategy-module flags** — operator can't tune ORB/scalp/session-break/vol-exp without reading source.

---

## 2. `docs/ref/env-vars.md` vs `.env.example`

### Drift findings

| # | Drift | Authority | Recommended action |
|---|---|---|---|
| 2.1 | **WRONG VAR NAME** — doc lists `TWELVE_DATA_KEY=...` for market data. Code reads `MARKET_DATA_API_KEY` (12 callsites in `services/market-data.service.ts`). `.env.example` correctly uses `MARKET_DATA_API_KEY`. | code | **Update doc.** Remove `TWELVE_DATA_KEY` row. |
| 2.2 | **WRONG VAR NAME** — doc lists `NEWSAPI_KEY=...`. Code reads `NEWS_API_KEY` (in `services/news.service.ts:15`, `apps/api/src/routes/intelligence.ts`). `.env.example` correctly uses `NEWS_API_KEY`. | code | **Update doc.** Rename to `NEWS_API_KEY`. |
| 2.3 | `.env.example` MISSING many vars from `env-vars.md`: all `FIRM_COLD_START_*`, `FIRM_EPOCH_ISO`, `FIRM_NORMALIZATION_MODE`, `LEGACY_XAUUSD_EXECUTION_ENABLED`, all `EXPOSURE_MAX_*` + `EXPOSURE_MIN_*`, `OANDA_MIN_UNITS`, `QDRANT_COLLECTION`, all `NEXUS_CIO_*`, `RAW_DATA_PERSIST_ENABLED`, `DAILY_MORNING_BRIEFING_ENABLED`, `POSITION_MANAGEMENT_ENABLED`, `STATUS_REPORT_ENABLED`, `FIRM_BLADE_PAUSED`, `DEMO_AUTO_DEGRADE_ENABLED`, all `AGENT_*` (Phase 0-2 bus + Phase 3 firm-agents), `FIRM_RESEARCH_DRAINER_*`, `AGENT_DISCORD_DELIVERY_ENABLED`, `DISCORD_CLARITY_ENABLED`, `FIRM_LOCAL_ROOT`, `FRED_API_KEY`, `FRED_CACHE_TTL_MIN`, `ANALYTICS_SINCE_ISO`. ~60 vars. | doc + sample | **Update `.env.example`** to include all production-relevant flags (with safe defaults). Single source of truth = `.env.example`, then have `env-vars.md` be the prose explainer that references it. |
| 2.4 | `env-vars.md` MISSING from doc but present in code/`.env.example`: `API_KEY`, `APP_URL`, `LOGTAIL_SOURCE_TOKEN`, `NOTION_API_KEY`, `NOTION_PAGE_ID`, `OPENAI_API_KEY`, `OPENROUTER_*`, `POLYGON_API_KEY`, `POSTHOG_KEY`, `QSTASH_TOKEN`, `SENTRY_DSN`, `TELEGRAM_*`, `TRADINGVIEW_WEBHOOK_SECRET`, `TRADING_ECONOMICS_API_KEY`, `WORKER_CONCURRENCY`, `FINNHUB_API_KEY`. | code + sample | **Update doc.** Or mark each as "auxiliary/optional, see .env.example". |
| 2.5 | `DISCORD_WEBHOOK_URL` is in both, but doc doesn't mention category-specific overrides (`DISCORD_ALERTS_WEBHOOK_URL`, etc.) that `.env.example` documents. | sample | **Update doc** to mention category routing. |

---

## 3. `docs/ref/known-issues.md` — 5-item spot check

| # | Issue | Status |
|---|---|---|
| 3.1 | Duplikat-rad fra `oanda-sync` backfill (548) | OPEN — `oanda-sync.ts:427` still writes `oanda_backfill_${tradeId}` rows alongside firm-path rows. No fix landed. Doc accurate. |
| 3.2 | Firm-path entry/SL/TP-drift mot OANDA-fill | UNCLEAR — `services/oanda.service.ts:271` reads `orderFillTransaction.price`, but managers.ts insert pre-fill flow not refactored to consume it. Likely still OPEN. Doc accurate. |
| 3.3 | `scalp_overlap_asia` gate wired to wrong path | OPEN — `firm/managers.ts:615` still hardcodes `strategyId: "xau-htf-trend"` for gate eval. The scalp-overlap strategy module exists at `firm/scalp-overlap/` but the gate isn't called from it. Doc accurate; recommend retitling the issue to "needs cross-path wiring after TIER 3 deploy". |
| 3.4 | `simulated_orders.strategy_id/desk` deprecated | OPEN — proposal landed at `docs/strategy/proposals/2026-05-08_deprecate_simulated_orders_strategy_id_desk.md`. Proposal not yet implemented. Doc accurate. |
| 3.5 | Postmortem hook (Resolved section but marked "venter på verifisering") | Code-side LANDED — `firm/postmortem-hook.ts` is fully wired into orchestrator at line 51 import, and `POSTMORTEM_HOOK_ENABLED` default=true. Has `postmortem_run_at` stamping logic. **Doc should move this from "Open"-ish wording to fully Resolved.** |
| 3.6 (resolved) | OANDA tx-history fix 4.5 | VERIFIED — `oanda-sync.ts:725` uses `transactions/sinceid?...` as the 3rd fallback. Doc accurate. |
| 3.7 (resolved) | OHLCV via OANDA | VERIFIED — `services/market-data.service.ts` has both Twelve Data + OANDA paths; `fetchCandles` uses OANDA-first per the resolved entry. |

**Recommendation:** Move item 3.5 (postmortem hook) up to "fully resolved" — code is in place, default-on, hook landed in orchestrator. Open issues 3.1–3.4 still accurate.

---

## 4. `docs/ref/firm-modules.md` vs `apps/worker/src/firm/`

### Drift findings

| # | Drift | Authority | Recommended action |
|---|---|---|---|
| 4.1 | Title says "**the ten** firm modules" but table lists 14. **And the actual filesystem has 16 sub-directories + 30+ top-level `.ts` files**. | code | **Update doc** title to "firm modules" (no number) and add missing modules. |
| 4.2 | Missing modules: `gates/` (TIER 3 new entry gates), `predictions/` (regression predictor / agent), `agent-knowledge/`, `agent-lessons/`, `scalp-overlap/`, `session-breakout/`, `vol-expansion/`. All have substantial code + tests. | code | **Update doc.** These are all live shipped post-04-25. |
| 4.3 | Missing top-level files worth a row: `decision-cycle.ts` (where assessThesisQuality lives — referenced by other docs!), `entry-thesis.ts`, `portfolio-brain.ts`, `foundation-gate.ts`, `drift-monitor.ts`, `oanda-sync.ts`, `firm-memory.ts`, `firm-epoch.ts`, `gate-impact.ts`, `range-context.ts`, `regimes.ts`, `strategy-blade.ts`, `strategy-execution.ts`, `strategy-snapshot.ts`, `shadow-log.ts`, `status-report.ts`. | code | **Update doc** OR rename doc to "headline modules + ownership map" so it's clearly not exhaustive. |
| 4.4 | "Decision path" block omits new TIER 3 path: strategy modules → `strategy-execution.ts` runs in parallel with firm-decision-path. | code | **Update doc** decision path to show both branches. |

---

## 5. `docs/ref/blackboard-topics.md` vs code

### Drift findings

| # | Drift | Authority | Recommended action |
|---|---|---|---|
| 5.1 | Doc says `xauusd.macro.fred` is listed **twice** (lines 10 and 23). | doc | **Update doc** — dedupe. |
| 5.2 | Doc MISSING from topic list: `xauusd.execution.fills`, `xauusd.position.opened`, `xauusd.position.closed`. All three are on `BLACKBOARD_AUDIT_ALLOWLIST` in `firm/orchestrator.ts:62-70` and used as topic literals in code. | code | **Update doc** to add these three position-lifecycle topics. |
| 5.3 | Doc says only `xauusd.position.events` for lifecycle. Code emits `xauusd.position.opened` and `xauusd.position.closed` separately. | code | Reflect in doc that lifecycle has 3 topics not 1. |
| 5.4 | Strategy module topics (`xauusd.orb.{range,state,signal}`, `xauusd.scalp.{signal,state}`, etc.) — all present in code as documented. | match | None |
| 5.5 | `xauusd.analysis.structure` — doc says "consumer-only, no publisher". Confirmed: only consumed at `conviction/adapters.ts:160`, no publisher. Accurate. | match | None |

---

## 6. `docs/ref/execution-paths.md` vs `orchestrator.ts`

### Drift findings

| # | Drift | Authority | Recommended action |
|---|---|---|---|
| 6.1 | Doc says firm-path is only via `firm/managers.ts:executionManager`. **Missing**: TIER 3 strategy-execution path. `firm/strategy-execution.ts` is imported into orchestrator and runs in parallel with `executionManager`, processing PROPOSAL signals from `xauusd.{orb,scalp,session-break,vol-expansion}.signal`. | code | **Update doc.** Add a third row: "Firm strategy-execution path → `strategy-execution.ts` → consumes module PROPOSAL topics → writes `simulated_orders`". |
| 6.2 | Doc only mentions XAUUSD legacy gate. Other markets (BTCUSD, EURUSD) marked as "legacy-path" but no live evidence either path is being exercised for non-XAU (audit material). | unclear | Spot-check operator. |
| 6.3 | Thread-state list reasonable; not verified end-to-end here. | n/a | None |

---

## 7. `docs/ref/critical-rules.md` — first 5 rules

| # | Rule | Code status |
|---|---|---|
| 7.1 | Firm-epoch filter applies to risk gating only | **IN FORCE.** `getFirmEpochIso()` used in exactly the 7 documented "DO filter" sites (`demo-mode.ts`, `exposure/drawdown.ts`, `managers.ts`, `risk.service.ts`, `supervisor.agent.ts`, `bot-manager.agent.ts`, `trading-manager.agent.ts`). API mirror at `apps/api/src/lib/firm-epoch.ts` exists. |
| 7.2 | Session windows DST-aware via `getLondonLocalTime()` | **IN FORCE.** `session-window.ts:58` calls `getLondonLocalTime()`. No hardcoded UTC hours found. |
| 7.3 | Session taxonomy split — uppercase enum vs lowercase liquidity labels | **IN FORCE.** `session-window.ts` returns `SessionWindowState` enum; `engines/session.engine.ts` returns lowercase labels. Distinct paths preserved. |
| 7.4 | Cold-start tuning env-gated, not code-gated | **IN FORCE.** All 4 `FIRM_COLD_START_*` flags read in `cold-start-config.ts`. |
| 7.5 | Neutral evidence contributes 30% to `forWeight` | **IN FORCE.** `decision-cycle.ts:200` has `dominantWeight + neutralWeight * NON_OPPOSITION_WEIGHT`. Comments preserved warning against revert. |

All 5 rules still in force. Doc is accurate. **No drift.**

---

## Summary

### Highest-drift docs (ranked by # of stale facts)

1. **`docs/ref/env-vars.md`** — ~70 missing vars + 2 outright-wrong var names (`TWELVE_DATA_KEY`, `NEWSAPI_KEY`). Highest user-impact: ops can't replicate Railway env from doc.
2. **`docs/ref/feature-flags.md`** — entire strategy-module flag families (~50 flags) undocumented. Firm-agent flags only in env-vars.md, not here.
3. **`docs/ref/firm-modules.md`** — title says 10 modules; reality has 16 sub-dirs + 30+ top-level files. Missing whole module families (gates, predictions, strategy modules, agent-knowledge, agent-lessons).

### Lower-drift docs (mostly accurate)

- `docs/ref/critical-rules.md` (first 5 rules verified in code)
- `docs/ref/known-issues.md` (4 of 5 spot-checks accurate; one item (postmortem) eligible for "Resolved" promotion)
- `docs/ref/blackboard-topics.md` (3 lifecycle topics + 1 dupe)
- `docs/ref/execution-paths.md` (missing TIER 3 strategy-execution path)

### Quick-fix doc updates (5 lowest-effort, highest-payoff)

1. `env-vars.md` — rename `TWELVE_DATA_KEY` → `MARKET_DATA_API_KEY` (line ~21), rename `NEWSAPI_KEY` → `NEWS_API_KEY` (line ~23).
2. `blackboard-topics.md` — dedupe `xauusd.macro.fred` row, add `xauusd.execution.fills` + `xauusd.position.opened` + `xauusd.position.closed`.
3. `firm-modules.md` — change title from "The ten firm modules" to "Firm modules"; or move the count to footer.
4. `known-issues.md` — move postmortem-hook item fully into Resolved with verified line refs (`postmortem-hook.ts`, default ENABLED=true, wired in orchestrator at line 51).
5. `feature-flags.md` — add a section "Strategy modules (TIER 3 — added 2026-04-26)" pointing to per-strategy config.ts files.

### Code-level inconsistencies surfaced (non-doc)

- `simulated_orders.strategy_id` + `desk` columns are 142/145 + 145/145 NULL — proposal at `docs/strategy/proposals/2026-05-08_deprecate_simulated_orders_strategy_id_desk.md`, pending Karri review.
- `scalp_overlap_asia` gate hardcodes `strategyId: "xau-htf-trend"` (`managers.ts:615`) so it never fires on actual scalp-overlap trades. Wiring bug — gate must move out of `bladeApproval` or get duplicated per strategy.
- `firm-path entry/SL/TP-drift` (known-issues.md#3.2): `services/oanda.service.ts` exposes `orderFillTransaction.price` but the open-position INSERT in firm-path is not consuming the fill-back value. Real fix is partial; reflects in r-multiple/percentile analytics.

### Obsidian note status

Written to `/home/nithu/Obsidian/Brain/00-claude-inbox/nexus/2026-05-11-doc-drift.md`.
