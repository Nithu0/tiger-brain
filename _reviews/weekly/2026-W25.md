---
type: weekly-review
week: 2026-W25
date: 2026-06-19
author: code-2 (ops)
---

# Weekly review — 2026-W25

> First weekly review (the routine itself shipped this week, per MEMORY-OS-AUDIT §13).
> Source-of-truth reminder: decisions → `_decisions/`, status → `<project>/project_state.md`,
> session knowledge → memory.db. Map: `_decisions/2026-06-19-source-of-truth-map.md`.

## What shipped
- **command-center / business**: managed-AI-ops product line defined — sellable offering is deep
  business-ops we build + operate for clients (Pål first via RefiPrep; finans + eiendom leadgen
  verticals; Verisure later). Strategy docs landed in `docs/ops/`:
  `managed-ai-ops-offering.md`, `sky-first-business-strategy.md`, `STRATEGIC-ANSWER.md`,
  `30-day-critical-strategy.md`, `START-HERE-leadgen-refiprep.md`.
- **RefiPrep MVP**: `packages/refiprep` exists (first client deliverable / build target);
  spec at `docs/ops/refiprep-build-spec.md`.
- **Leadgen verticals**: source maps for two verticals —
  `docs/ops/leadgen-finans-sources.md` + `docs/ops/leadgen-eiendom-sources.md`.
- **Memory-system audit**: `docs/ops/MEMORY-OS-AUDIT.md` — 4 parallel audit agents read real code +
  live memory.db. Core finding: the Memory OS is ~90% built, ~10% wired (capture + recall are
  disconnected from the session loop). 30-day plan + first-10-tasks defined.
- **Memory bug fix**: distill pay-and-discard bug fixed (`clampExchangeCore`, memory-engine) —
  exchanges >400 chars no longer hard-throw and waste the API spend (bug #3, FIXED 06-19).
- **Source-of-truth cleanup (this task)**: weekly-review routine + template created;
  consolidation note added to `_decisions/`; stale CLAUDE.md status footer corrected.

## Decisions made
- **Source-of-truth map**: one authoritative source per knowledge type — memory.db (session
  knowledge), `<project>/project_state.md` (status), `_decisions/` (decisions), CLAUDE.md
  (stable rules). → `_decisions/2026-06-19-source-of-truth-map.md`.
- **No hardware purchase now**: audit §10-§12 — close the memory loop with the existing stack
  (laptop + Railway + Claude + sqlite-vec + Ollama embeddings); buy nothing. → MEMORY-OS-AUDIT §17.
- **Keep sqlite-vec** (no Qdrant/Chroma/pgvector now): at ~7.5k vectors vs ~200k ceiling. → §8.

## What was archived / superseded
- Nothing moved this week (archiving is operator-gated). Flagged-as-superseded list lives in
  `_decisions/2026-06-19-source-of-truth-map.md` (old May-14 proposal/feasibility decision notes
  now realized in shipped code). Operator to move to `90-archive/` later.

## Cost / retrieval drift
- API cost: ~$0 this week — capture is broken so the distill loop is starved (not actually $0 work;
  it's a broken-meter $0). Expected ~$10-45/mo once capture is fixed. $42 was a one-time backfill.
- Retrieval health: **memory.db verbatim frozen since 14 June** — capture not firing (audit bug #1).
  MRR=0.9556 is UNVERIFIED (measured with a fake embedder); must re-run against real bge-m3.
- Drift flags: 4 bugs found (capture dead; recall uncoupled+crashing+masked; distill
  pay-and-discard [FIXED]; project-scope leak via post-ANN JOIN). 1 of 4 fixed.

## Next week's ONE goal per active project
- **command-center / memory**: fix session-capture (#1) so memory.db stops being write-only — the
  single highest-leverage fix; everything else is downstream.
- **nexus**: hold current strategy; no auto-disable; operator-gated changes only.
- **thesis (Master-oppgave)**: continue thesis writing/ML pipeline (no memory-OS dependency).
- **AS**: regnskap/inntekt — no change driven by this week's work.
- **soking-fulltid**: draft-only job pipeline; canary retrieval query is the test for the
  project-scope-leak fix (week 3).
- **personlig**: no active goal this week.
- **refiprep / managed-ai-ops**: advance RefiPrep MVP toward Pål as first client (per
  `docs/ops/START-HERE-leadgen-refiprep.md`).
