# _promote-candidates

Claude WRITE zone. Notes proposed for promotion to repo `docs/memory/promoted/`.

## Workflow

1. Claude (or operator) moves a worthy inbox note here
2. Operator reviews + polishes
3. Operator (or Claude with explicit "OK kjør") writes the polished version to:
   - Nexus → `/home/nithu/code/ai-assistent/docs/memory/promoted/<slug>.md`
   - Thesis → `/home/nithu/code/Master-oppgave/docs/promoted/<slug>.md` (or appropriate path)
4. Promoted candidate is removed (or archived)

## Naming

`<YYYY-MM-DD>-<slug>.md` matches inbox naming for traceability.
