---
source: youtube
video_id: 9lKkAXTh2vs
url: https://www.youtube.com/watch?v=9lKkAXTh2vs
title: "How to Build an AI Voice Agent Using Pipecat (feat. Daily.co, Twilio, Recall, Tavus, Flutter)"
project: call-center
fetched: 2026-06-22T23:41:38Z
words: 8015
---

# Transcript — How to Build an AI Voice Agent Using Pipecat (feat. Daily.co, Twilio, Recall, Tavus, Flutter)

Now, I'm going to call this. So, now I'm receiving this call. &gt;&gt; Hello. Thanks for calling. How can I assist you today? &gt;&gt; Can you tell me what are my upcoming events on my calendar? &gt;&gt; You have a meeting with John on April 6th at 8:30 p.m. and a meeting with Rashid on April 8th at 9:00 p.m. Anything else I can do for you? &gt;&gt; So, yeah, you can see those are the two meetings which I get and this is what I got it here. Hello, everyone. In this video, we are going to learn how to build a voice AI agent using Pipe chat. And in this video, we are going to cover how to create an agent using Daily co, how to add tools to it, how to create a live transcription using Observer, how to create custom web socket for any form of input and output in form of audio or video, and how to create an agent via phone call, how to make agent join the meeting, how to create an agent with a human avatar, and how we can add that agent with human avatar into any app. And this is not going to be a step-by-step tutorial. I think most of the AI agent can do that very well now. But in this video, we are going to showcase how the things get connected, how all other services are integrated to each other to make all these things work very seamlessly. So, why Pipe chat? First of all, we need to understand to build any voice AI agent, the biggest challenge is the latency. And to make the voice AI seamlessly work, we need at least 500 ms of the latency. That is, whenever user speak and they get the answer, we need that 500 ms latency to get the answer to the user. And this require a lot of orchestration. For example, getting the user audio as an input and getting the output from the bot to the user, converting that audio into text, then passing that text to the LLM, which requires to use different tools, get that answer back, and then provide that answer as an audio, and then that audio should be passed through as an output to the user. And all these things should be happening in a real time and also in a 500 millisecond time frame. And to do that manually, it's a really challenging. And Pipe Cat does all this orchestration for us. And the reason why I like the Pipe Cat most is because first is open source, and second, it has a pluggable system. Basically, you can add any LLM available in the market like OpenAI, Anthropic, Azure, Gemini, and many other services. For example, to add a human avatar, you can use a service like called HeyGen, Tavus, and others. So, yeah, that's why I like Pipe Cat, and we are going to start with the first example uh with using Pipe Cat. All the example in this video is available in this repo. Uh you can check out the back end and check the example for each which we have explained in the video. Okay, so let's start with a basic example of using Daily Co with the Pipe Cat. So, first I want to show how it looks. So, I'm going to run our web app. And then I'm going to run my back end app. Now, this is how it look like. So, when I click on a start board, it will create a Daily Co meeting, and and then the bot will join, and if I talk to the bot, it will respond. So, let's see. &gt;&gt; Hello. How can I assist you today? &gt;&gt; How are you? And what you can help me with? And what's your name? &gt;&gt; I'm just a program, but I'm here and ready to help. Let me know what you need assistance with. &gt;&gt; So, you can see in this back back and log that we are getting this input transport and transcriptions and all that stuff which I'm talking to and this is all how the pipe guide is doing. So, now let's get into it how it works. So, before you start, what we need is we need API keys to get this working. So, for this we need daily API, open API, Cartisha and voice ID. Okay, for daily call you go to the daily call dashboard, you simply log in and then you go to the developer console in that developer console you can create a new API key or copy the existing one. And we need to use that in our app. So, for open AI you need to go to the open AI platform and then you need to log in and once you log in you will have API keys and then you can create a new API key, give it a name and copy it and use it in our app. To get the Cartisha key you need to go to their platform, log in and then go to the setting page where you will see the developer API key. You create a new API key, you copy that key and for voice you go to the voice section and select multiple voice you have and you just copy that voice ID. And we just need to paste those here. This. So, what I will I'm going to go here, copy it here. Then you need to get a daily API key. So, this is my daily API key and this are my Cartisha key and this is the voice ID which you can select on whatever you want to have that voice ID on. So, this is how it works. When you click on start board it calls the start board API here which is our daily board start and what it does it it's create a token with a daily call with the helper of API key which we created and that token is passed to this create a sync task and this run board function. And in this run board function, we get a room URL and a token, which we pass through the daily transport. And this daily transport is a framework part of the pipe cat. And this basically transport handles the input and output of the audio. So, whatever speaking in the meeting will be passed through a pipeline. And this is the board name, and we want to audio in and audio out enable. And we want to have a this word analyzer, which basically analyze when to speak and when not to speak. It's kind of a user strategy and all that stuff. And it wait for 0.8 seconds to respond back from the board and from the user. Then we use this open AI assistant service, which basically translate speech to text. And then this LLM service, where we have our LLM setup. And then the Cartesia service, which basically convert the text into the speech. And then this is the message, like a system message, where we are saying that it's a friendly and cool voice assistant. And we pass this as a context in open NLP API. And this is the really important crux of the pipeline. The orchestration which is happen here, that we take input from the transport, the audio frames, then it converted into text, and then we pass this context aggregator as a user. Then we have this LLM, which basically does all the processing. And I mean, kind of our AI agent, which basically query answers all those questions. And basically heart of our pipeline. And then the TTS service, whatever things LLM replies, we convert that into speech. And then we take that as an output, so the board can speak as an output. And then this basically take all the context together, so that the conversation stay in the context. And then we create this pipeline. And then we run this pipeline. And we have this event as well, that on first participant join. So, whenever we join, the board send greatly warm message. And when we left, we can do some clean up and all that stuff. So, this is how basically the Daily co is working with the Pipe cat. Now, the reason why it took only like, I think, 60-70 lines of code to make this work because uh Pipe cat has been created by Daily co and they have this really seamless integration with the Daily co. So, yeah, that's how it works. So, now all these things work, but then how this meeting gets rendered is that when we call this start board API. So, we return this room URL, and when we return this room URL in the web, we have this Daily room component. And what happened here is that when we get that URL, we create iframe, and then just embed that URL with the token here. So, it's basically rendered in a iframe. And the Daily co basically handles all that uh handling of events and everything and rendering in this iframe. So, we are just using that iframe in the front end, and nothing else. So, it's pretty much a straightforward to do the integration with the Pipe cat for the Daily co. Now, let's get with our second example. What we have done so far is we have integrated the bot in a meeting, and it just speak whatever we ask for it. But, it's pretty much useless until unless it can do something. And that's where this Pipe cat really shines is that you can integrate any tool you want into your pipeline, and it will respond back based on those tools available. One of those tools is we are going to showcase here is Google Calendar. Okay, first you need to log into the Google Cloud Console, create a new project, and in that new project you create a new service account. You follow this instruction to create your service account. And once you have that, you can check the sample on how I created the service account and how it look like. And then we will get an email. You copy that email, and then you go to your calendar, and you create a new calendar called AI assistant and in that uh you create and share and manage and in that share and manage you paste that email, allow you to edit and update and just save it. So, yeah, that's how you share calendar using Google Cloud credentials. Now, to integrate a Google calendar, we need a Google calendar service account and Google calendar credential. So, once we have that, we can add that credential here and the Google calendar ID here. Now, let's run the both app. I'm going to run the back end and then I'm going to run the web app. Now, we have a tool chain integration here. Now, you can see I have a calendar here which is shared to the AI assistant and then I have this support here. So, I'm going to click start bot here. I'm going to mute myself, so it will greet it out. Meanwhile, we can see that here we have the back end logs where &gt;&gt; How can I assist you today? &gt;&gt; So, he's basically greeted and now bot stops speaking. Now, I'm going to say here that schedule meeting with John 3:00 p.m. tomorrow. Please schedule a meeting with John at 3:00 p.m. tomorrow for 30 minutes. Now, as you can see here, you will get the log here. Uh please schedule a meeting then &gt;&gt; I scheduled a meeting with John at 3:00 p.m. tomorrow for 30 minutes. &gt;&gt; So, you can see here it calls the tools and I'm going to explain how this tool works, but it call our tools and then create a meeting here. So, you can see I can see the 8:30 meeting with John here when created. Now, I'll just stop this for now. So, how this tool work is that first you need to set up a pattern on how the calendar works. So, this is basically our calendar API which you can check from the documentation on how to create events and list event. However, the important thing here is that we how do we register as a tool? So, the LLM required a list of tools here, and you need to define what kind of type it is. It is function, and then its name, and then its description. And this is important because LLM basically thinks of this description and try to call appropriate function. And this is basically our function name. And then what kind of parameter we need to call this function? So, here we define what we needs to have in order to create event. So, if it's not there, the LLM will ask user to give those information and create an event. Similarly, for create event, it needs more things like summary, description, start time, duration minutes, and all that stuff. For example, I say for the 30 minutes. And this get upcoming event, this is the function signature. It takes a parameter and then calls the API and return the result to this result callback. And based on that result, it create another message and reply to it. Similar to create event. So, whatever function you want, you can register here. And then when you come here in OpenAI LLM context, which we saw earlier, we didn't have this calendar tools here. So, now we added this tools. So, now it has the context, and we also give this system prompt that do you have access to Google calendar? And whenever someone asked for the calendar, you just call it out and perform the action necessary on this tools. And this is the same thing happening. It's just that we add the additional tool in the LLM. So, this is the heart and this are the kind of a veins pumping to the pumping bloods in the whole pipeline. So, this tools are really important, and it can connect to any tools in your knowledge base. If you have some internal documentation or internal knowledge base or something like that, you can give those access through the API, and it will take care of calling those function accordingly. Let's move to the example three. Whatever things happening behind the scene we have seen so far, but how do we update things on the UI? So, here what we are going to do is we are going to be Firebase and whatever happening chat is happening and what is the event is happening, we are going to update that in a real time in this UI. Now, get the Firebase service account and then Firebase data URL. And this is Wi-Fi where is the URL? So, it will be used on front end as well and and the back end to push the event and to receive the event. Uh Firebase key, you create your project, click on that and then you go to the setting page and in that general you create the service account and then you download your uh private JSON key from there. And for real-time database, you click on that real-time. You need to enable it first and then you copy that URL and then paste it in our app. So, let's start on this. So, when I click on this start bot, it will join it and it will greet it. And as you can see that when this is happening, uh we can see the update this on our UI. Can you tell me what are my upcoming events? So, I have asked this question. Now, it's the same calendar. It will go through it. And I hope it will answer. So, you can see the tool has been called. Now, this is the real-time transcription happening and how this happened is that this is connected to Firebase real-time database. And let me just go through what happened here. One additional thing the Pipe Cat has is called observers. So, we have created Firebase observer here. We have done the init state and getting a session reference ID to manage the each session. So, that is more of a logic, but here the important thing is that we have a Firebase transcribe observer, which basically observe all the event happening inside the Pipe Cat pipeline. So, we don't insert this in between the pipeline, but is just more of a side effect on what's happening inside the pipeline and based on those event, it get push things on. So, for example, we get an on push frame for this base observer. We can basically figure out which frame it is. Is it a transcribed frame? Is user stop speaking? Is it a element full response start frame, end frame, and all those things. So, what here we do is we do a logic where, for example here, if the tool is being called, we send a message called write message on the Firebase real-time database. And it update those things accordingly out of here. And here we get a user stop speaking and all those frames come here and based on those calculation that whenever user is speaking, the message the transcribe we get, we pass that here in the Firebase. And based on the user answer we get, we pass that as answer. And hence, we get this whole chat session here and it gets updated in Firebase real-time. As you can see, we have each new session and this being active, there has been a lot of sessions since I was testing this out. And we update those things on on those UI accordingly. Now, where do we register this observer is that we come to this pipeline and here in the pipeline we have a parameter called observer and we can do bunch of observer with a different different responsibility. For example, if you want to track analytics on a different database or if you want to do some different behavior, a side effect on this pipeline. For example, inform or email a user when their task has been completed or a new ticket has been created. You can do some all of those side effect using this observer. So, we create this observer, Firebase observer, and before that we create this new session, pass the reference ID just to update and keep things on that session, and we pass this observer here. And the PipeCat took care of passing all the different frames and all different observers to here. You can also add a logging mechanism. PipeCat comes with a bunch of loggers and supports for debugging. But if you want to enhance that, you can enhance that using observers. So, this was the one concept I want to show that based on observers, you can side effectively update the UI here. Now, what happened in the front end is that we have observer UI where we initialize the Firebase app. And when we create the start bot, we respond back with a session ID. And in that session ID, we just basically, you know, transcribe and it's get updated in real time. So, here we use state and here is a transcribe and every time the message is updated in this message, it's get updated. So, it's just that here Firebase handles snapshot where on any changes happen, it's update the message and it's update on the UI using states in the React. So, that's how it it works on front end side. And this is as usual our iframe is here to showcase the meeting URL there. So, our next example is using custom web socket. What we earlier saw was having Daily co integrated and doing all handling input and output. But what if we want to handle the input and output ourself? For example, let's say I have a mobile app and I want to control the input coming from the mobile like people directly speaking to the mobile and also getting the output on the mobile. So, how do we do that? And in the PipeRider, it support custom web socket and first I'm going to show you how it look like. What I'm going to do is I'm going to run the back end here and then I'm going to run the web app here. So, if you go here and refresh it, you see currently we are running a web socket transport, I mean a web socket our locally and this is the URL. Now, I have created a Flutter app which is going to run in my mobile app. So, now I'm going to run this app and uh it will show it up. Now, as you can see that it directly got connected to the web socket because uh it gave the URL from the back end. And now I can talk to it. So, if you see I'm concerned over web socket. So, now whatever I'm saying it's picking up it here. Now, I don't have anything on the calendar. So, let me just say Hey, can you please create a meeting with Rashid on April 12th at 3:30 p.m. &gt;&gt; How long should the meeting with Rashid be? &gt;&gt; The meeting should be 30 minute long and it should be on 2026. &gt;&gt; The meeting with Rashid has been scheduled for April 12th. &gt;&gt; So, yeah, you can see it created a meeting. There is some time zone issue, but we can fix that. But, this is just to showcase how this whole thing works. Now, let's get into how these things work. You can see the messaging here and all. This is basically as we see earlier is connected to Firebase. So, this is connected to Firebase real-time database. And in the mobile also we are connected to Firebase real-time database. So, what's happening here is that we are creating a local web socket which is connected to this port. And our mobile also get connected to here and it sends voice packet to this web socket and and it works. So, first of all, uh what happens here is that we have a back end. In the back end we have called web socket info which basically gives the web socket URL and the session ID to the mobile to get connected to. And then in the mobile app uh we have this web socket screen which basically get on started and it called that same API and it get connected to that. So, here it got started. This is for the microphone and in the uh widget and in the services we have a pipe cap web socket which is basically a wrapper on the web socket. It get connected here with the web socket URL which we get from the backend. We have a backend client config here. And and that's how it works. However, in order to make your app work, you need to make sure that you have proper setup Firebase URL which is basically Google service here. And also in the asset we have this config.example here. So you give a web socket URL and all the Firebase related key in this config.json and you run this app. So that's how it get connected and knows the our backend URL. So this pipe cap web socket is responsible for getting connected and then it starts the mic here and it's using a flutter PCM package which basically listen to the microphone and convert into PCM audio and pass it into your web socket. And we have a start mic when the web socket get connected and we have defined this config and it continuously stream those chunk and then it basically pass through the web socket as an audio raw frame. Now in the backend whenever web socket get connected we call this run board. It has the host URL and session ID and initialize the Firebase as we have done earlier. And here we have a transport input called web socket server transport. And this is the class given by the pipe cap so that you can create your custom web socket and pass the audio directly here. It has all the features for the web socket parameter which we have for the daily call, bad analysis and all stuff. Here I'm using inbuilt protobuf frame serializer which basically gets the protobuf and serialize it. You can create your own custom serializer which will look into other example. And this frames protobuf frame serializer is coming from a mobile here. We have defined this protobuf here and we pass it to the web socket and it basically reads that. So, kind of a JSON serialization but done in a proto format. Now, when we come here, this is our main web socket which basically handles the input and output. Same SST LLM TTS service. We register our function our tools to get the calendar event and create an event. And then we have this pipeline set up here which is basically input and the output. Now, it's become our web socket and then we run the pipeline. We have an observer which is our Firebase observer and updates on the UI. And that's how this all get connected and when we cancel the API, we call the stop bot and same thing happened again. And our web app is just basically to show that you can update on multiple parts. It's just showing that the web socket is running and this is the session ID. You can create a new session and all that stuff. So, it's just a basic example show but in this example, we actually don't need this to have a web app but for an example purpose, I have kept it here. Now, this allows you a lot of flexibility. Not just in a mobile app, you can have that in your web app, embedded device, anything. Wherever you can have a web socket, you can basically transport that audio as an input and you can use the same web socket to listen to an output and play the audio on the mobile or any other devices. So, yeah. That's how flexible PipeCat is and that's how this whole mobile end-to-end flow works. Okay, so in this example, we are going to use Twilio on making an actual phone call and then receiving the query and giving the output from our calendar event. Um so, the reason why I'm showcasing Twilio here is that in earlier example, we used directly web socket to get the input and producing the output. But here, the web socket is being handled by Twilio and the PipeCat support Twilio serializer which we are going to use it here and use their web socket to manage that. So, here we have the documentation of this Twilio web socket works. We need to go to their console and and log in it. And here in the environment variable, we need a Twilio account SID or token and Twilio phone number, which you can get uh from their dashboard. So, for Twilio, you go to that Twilio console. Uh you create your account. And as soon as you create account, you get landed to the dashboard. And in that dashboard, you can copy the account SSID and also the auth token and the phone number. And we use that in our Once you have done that, you need to go and verify cell number. And these are the phone number which you can use to call for testing purposes. But when eventually you want to call it things dynamically, you need to go to the paid plan and and then do all the logic there. So, I have added my two number here, get an OTP and verified it. Now, one thing here to notice that since uh Twilio communicate all those things via web socket, and it require a real web socket connection to do that. Currently, we are not hosting our application, so we need a tunneling mechanism to get that web socket data into our local, and then we can test it out. Which is basically called tunneling. And to do that, we have a tool called ngrok. And I'm going to run that on our local port, which is in our backend is going to run. You can install that through the CLI. And then it will run this. And now we'll get a public URL, which whoever will access will directly get uh incoming request to our local host. So, I'm going to copy this. Mhm. Paste it here. And I'm going to run my backend on the same port, which is important. And then we are going to run our web application. Now, I'll come to here and I'll just refresh this. So, this is our web application. I need to enter a number, and then it will call that. So, one thing we need still need to do is we need to create this Twilio ML bin, uh which requires this websocket connection URL. So, I'm going to go here and we are going to tell Twilio that this is the websocket connection you should be sending your things to. You can create a new one, but I have already created this one, so I'll just go to my VS URL and then uh here we need to give our ngrok URL, which I just copied here, and save it. But, usually uh in production you will have uh production URL, staging URL for different different websocket connection, and you will going [clears throat] to have those things all set up. But, since we are testing locally, uh we are passing that websocket URL. So, now it's done and I'll just add one of the number here. Now, I'm going to call this. Now, as you see uh the session got connected and I'll be receiving a call here. So, now I'm receiving this call from the Twilio. &gt;&gt; Okay. You can remove this message at any time by upgrading to a full account. Press any key to execute your code. &gt;&gt; So. &gt;&gt; Hello. Thanks for calling. How can I assist you today? &gt;&gt; Can you tell me what are my upcoming events on my calendar? &gt;&gt; You have a meeting with John on April 6th at 8:30 p.m. and a meeting with Rashid on April 8th at 9:00 p.m. Anything else I can do for you? &gt;&gt; So, yeah, you can see those are the two meetings which I get and this is what I got it here. Now, how this work is quite interesting. When I do a call, calling that API here is not doing anything fancy, just fed the phone number and call the API, this handle call API, and get the status status status from it. And in the backend, we have this API phone bot call and we are having two as a phone number. Now we are basically getting all the credential for the Twilio and then we are calling this Twilio API. But to call this Twilio API, we need to give this ML URL which we build it here based on our current endpoint and pass it here. So this is basically built from the ngrok which we provide it here. And [clears throat] it will be passed with that session ID. And then we'll use a make call which is the part of Twilio API and it will call the Twilio API and it will just initiate the call. Now why we pass this URL is that whenever the call happens and as I received it as soon as I received it and verified it, it will connect to a web socket and to connect to a web socket, it since we pass this URL it will call back here as a get and it will get that web socket URL here. And then it will get connected to our web socket. So it's just a mechanism to have a handshake to connect the web socket. And once that web socket connection is done, we have running this web socket locally in our system. So I'll get the session ID and phone ID session from that web socket and as soon as this web socket get connected we'll run pipeline through pipechat and we'll get this web socket object from the fast API and we'll pass that to this fast API web socket transport which we only had had for the mobile web socket which was our local one but now it is for the Twilio and we'll have we have this Twilio frame serializer directly from the pipechat. We are not doing anything here passing this credential and using this as a serializer. Now this Twilio becomes our input and our output. And then all the open AI city open AI LLM all the same we register our function here to get the tool calling and show the data there. This is the prompt and this is the main orchestration pipeline where we are getting this Twilio input and we are also providing output by having this TTS service. And then we have observer which basically update the Firebase real-time database and update on a UI. And then this keeps running until unless the web socket get disconnected and it will get cancelled out. So, So yeah, that's how this whole system work on calling API then handshaking the changing exchanging the information in a handshake manner to get the web socket connection URL and then it connected to our web socket locally and then we get that data and through the So yeah, that's how this Twilio works via Pipe Cat. Okay, so let's start with an example number six where what if we want to use a web socket connection for input and output but it's not something we control. It's coming from a third-party API or a service and we don't have a serializer built in the Pipe Cat framework. Similarly, what we did in Twilio which we it had earlier inbuilt but in this case we don't have. So, what we are trying to do is we are going to use a tool called Recall AI which allows you to join a bot in any meeting, listen to the audio and then basically provide the output. And in order to integrate Recall, what we need here in the environmental file is Recall API key, Recall AI region and a base web socket URL. Now, I'll explain what this is. Uh you go to the Recall platform and then you select the zone. You need to copy that zone ID as well and then you go to the developer side and then you create a new API key. You name that, you copy that key and then paste both that API key and the zone key into the app. Now, what this web socket URL is that whenever you join a meeting, Recall provides an audio chunks in real time. But you need to provide a web socket URL to receive the whole audio chunk. Now, this URL would not be a local host, so we need to use ngrok for that. So, here we're going to create new terminal and I'll just run ngrok. And it will generate me a URL which I'm going to copy it here. So, this will become a local host will become a real-time web socket connection here. Now, we have this URL and let's just see the demo on how it work. I'll just run my back end here. Now, I'm going to create a meeting here. And I have joined this meeting and then I'll just provide this URL here. And I'll just send that bot to the meeting. Now, the bot is in the meeting. So, I'll get kind of a request here. So, I'll just add this Pipe Cat bot. And it will add it here. Now, currently I'm mute, but whatever I'm going to say would be listened to that bot. So, here's my calendar. I don't have any event, so I'll come here and say, "Hey, can you create a meeting with uh my friend called Rashid on, let's say, 8th of April for 3:30 p.m. for 20 minutes." Now, as I have said it, you can see you can get the message here. I can get Can you please provide the year of this meeting? So, I'll say uh 2026. So, sometimes you need to write a prompt in a way where it gets all the information. And here it's there. So, if I come here and this is March. So, it's 2024. I'll go here, 2026, and you can see John meeting with John and meeting with Rashid. There's some time zone issue. So, you need to fix that, but this is how the tool calling and all those things work. And this is how the pop uh Pipe Cat bot can respond to uh your query here. So, &gt;&gt; [snorts] &gt;&gt; uh this is how it works. Uh now, how it works is that whenever we call this send bot to meeting, so in this back end, uh we go to the main and here we have this meeting.bot. Where is it? Start. And what we do is we got this re- recall API and we call send bot to the meeting, which is basically uh the recall API here, which we are using, and it create all this configuration, the web socket URL which we create, and is just pass it to that. We have other helper method, get bot status, remove bot from the recall API. So, we just added in API for all those things. So, we see that status, the bot joining and all that in in real time. And also we create a web socket. Uh we are using fast API. So, this is the web socket with the session ID. And as soon as we get connected, we call the run uh recall bot that session ID. And similarly, we set up our pipeline. And here is the important thing. Since this is the third- party web socket, which we don't control, but we received it, but we need to pass it because the Pipe Cat doesn't support that. Here we using fast API web transport, then we are passing the web socket information which we had. And here is the important part is that recall frame serializer. So, here in the frame serializer, we manually like custom serialize it and deserialize it. Whatever we get, we get this and pass this as the audio frame if someone is want to listen to the output. And this is how we deserialize. So, whenever you get an audio, we deserialize. So, this is how we write the custom serializer and deserializer for something that PipeCat doesn't provide support for and we want to do something custom and it's coming from a third-party API. And then this transport become this is something similar OpenAI and Cartisha TTS service. Similarly, we this is the message, we access give to the tool, and this is the same PipeThing. So, all we needed is just we needed the transport input, we change it. And this is the transport output, which will deserialize it and send it. Currently, we are not receiving the audio and and showing it it. So, but if you want, you can listen to this web socket, wait for this audio chunks coming in, and then you can play that audio in in real time. So, yeah. I mean, this is how this is currently Recall working. And on the front-end side, you don't have to do anything. You just call this API by passing the meeting URL and listen to the Firebase as an observer and everything gets updated. So, yeah. This is the main crux of how to create custom serializer for your own custom needs. Okay, so let's move to some more advanced stuff where we have been working with the bot so far just from audio perspective, but what if we want to introduce an avatar video bot into it? So, the PipeCat support this many transports and one of them is Tavus. And if you go into the video, PipeCat support HeyGen, Symbl, and Tavus. So, in this example, we are going to use Tavus as a video avatar. So, to set up that, we need Tavus API key, Tavus replica ID, and that was persona ID, which I have pasted it here. Now, uh for Tavus key, you go to the Tavus platform. You go to the API key section on the sidebar. You create a new key. You copy that key. And also, you want to have persona for that. So, you go to the persona list. Select any persona you like, and then you create a replica ID, and you also copy the persona ID, and then we use both that in the app. I'm going to uh run our back end, and then I'm going to run our front end. Now, I'm just going to refresh this. Now, you see we have an example. So, when I click on start avatar, what would happen is that it open up this voting to join the meeting. Once we join this meeting, uh we'll have this support. Hey, can you tell me about yourself? &gt;&gt; Yeah, totally. I'm the Tavus AI assistant here to help you figure out if Tavus is a Let's say and what it would look like to use it. &gt;&gt; Yeah. So, that's how the bot speak. The meeting you saw is basically a Daily co meeting. Now, what happened behind the scenes is we call the API here, Iframe start. It gives the conversation URL, and then we run this bot with this conversation URL. And instead of Daily co transport, we are used Tavus transport. But, behind the scene, Tavus used Daily co as a WebRTC mechanism to join its bot and have the communication with it. That's why we see Daily co uh meet when you want to join it. And we see the same thing. We enable the transport for Daily co. We have this STT LLM service and all those tools which we have done earlier, similar to that. We have input, and then we have output. Since we are using uh Daily Tavus transport, it will basically gives the output to the bot, and it speaks. And all the, you know, mechanism of handling board movement and it's basically part of the Dylos platform. So, that's how it basically does and it gives this intro message and all that stuff. So, yeah, that's how this works and I think it's cost way too much for Avatar to have in a board compared to different audio boards. And if you go to the front end, how it loads is similar to how we had with Daily Co. We give the URL in the response back and it basically use an iframe to render that. So, that's how it basically loads in front end. Now, the next example is currently Dylos used the Daily Co inside, but what we don't want to have a Daily Co inside it. What if we want to control that board in our app wherever we want. We don't want to have this join button calling in and all that stuff. So, we can do that. For that, we need to have this Daily Co JS library in the front end and then all the events which we get we can pretty much handle that. So, I'm running this example here. Let's just see how it looks. So, if I click on start Avatar, now instead of having that Daily Co meeting, it will render this whole UI here. And this is the room ID and this is how the board actually is look like and based on that it responds. So, yeah, I mean, we have to connect the audio and all that stuff. Since we are doing manually the WebRTC handling, we have to do it all by ourselves. So, how this work is when you click on the start Avatar uh I'll just show in the main here. We call this create conversation this is basically in Dylos API, create this with replica and personal ID and give us the conversation URL and based on that, we just create a task and run our board since same the transport Dylos transport and all that stuff. But now instead of loading the iframe in the Daily Co, what we are doing in the front end is we have installed this daily go package file which allows us to have all those custom things on the front end and then we have this WebRTC. And here we use this uh daily go to create an object of that call, get the URL, save that call here and we use different event on on join to basically update so on a UI. And whenever the track start is stopped. And whenever the user joins this meeting, we click join and have that conversation URL. So we only see that bot in that iframe. And basically we load that here as a video. We just give that as a video reference and we basically show that here. So that's how we can do custom thing on our WebRTC. And if you want to go more custom route or handling videos by all yourself, Pipe Cat has a support for that as well. Like this small WebRTC, you can create your own and do bunch of stuff, but you have to do those things manually and take care of clinical session, managing a session and all that stuff. And you can try different tools like Haze and similar tools and other to play around and see how it looks and how it works in your use case. &gt;&gt; If you are overwhelmed by with amount of code you have seen in this video, then don't worry. I have open source this repo voice AI Pipe Cat example here and explain the structure on how it works for each example. We have a bot and router and tool and observe for it. So you can directly run this and check it that out. And if you want to go for the specific example, you can branch out to like specific example for here example five Twilio and you can go through the project structure, set up, how what keys to be set up, how to run the back end and how it works, the walk through and how the session life cycle work, the Pipe Cat pipeline, everything is there to give you a walk through on how this example works. And if you want to go beyond that, then Pipe Cat example is a really good report to see bunch of example. For example, your function calling you will have a lot of example here. For real-time things, you will have a lot of example here. For videos avatar, you will have a lot of example here. So, yeah. Uh check this out the repo for your need and the PipeCat one if it is required. And yeah, if you have any question then put it into the comment section or open an issue in this uh repo. I'll happy to answer. And if you like this video, then don't forget to like, share, and subscribe. Thank you for your time for watching this video. Thank you.
